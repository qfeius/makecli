version: 2

# -------------------------------------------------------------------------
# 构建前置：确保依赖整洁
# -------------------------------------------------------------------------
before:
  hooks:
    - go mod tidy

# -------------------------------------------------------------------------
# 多平台构建：通过 ldflags 注入版本信息（与 Makefile 保持一致）
# -------------------------------------------------------------------------
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/qfeius/makecli/internal/build.Version={{.Version}}
      - -X github.com/qfeius/makecli/internal/build.Date={{.Date}}

# -------------------------------------------------------------------------
# 归档格式：unix 用 tar.gz，windows 用 zip
# -------------------------------------------------------------------------
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

# -------------------------------------------------------------------------
# 校验和：方便 Homebrew formula 验证完整性
# -------------------------------------------------------------------------
checksum:
  name_template: "checksums.txt"

# -------------------------------------------------------------------------
# Release：tag 带 semver 预发布段（v0.6.0-beta.1）自动标为 GitHub prerelease，
# /releases/latest 与稳定通道用户（含所有旧版本二进制）天然不受影响
# -------------------------------------------------------------------------
release:
  prerelease: auto

# -------------------------------------------------------------------------
# Homebrew Tap：GoReleaser 自动向 homebrew-makecli 推送更新后的 formula
# -------------------------------------------------------------------------
brews:
  - name: makecli
    # 预发布版本不推 Homebrew formula：brew 用户永远只见稳定版
    skip_upload: auto
    repository:
      owner: qfeius
      name: homebrew-makecli
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/qfeius/makecli
    description: "makecli — agentic development platform cli"
    license: MIT
    install: |
      bin.install "makecli"
    test: |
      system "#{bin}/makecli version"
