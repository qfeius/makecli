# cmd/
> L2 | 父级: /CLAUDE.md

## 渲染约定
- 表格统一用 `github.com/olekukonko/tablewriter`（边框 + Header），禁止用 stdlib `text/tabwriter`
- key-value 头部信息（File / Name / App 等）用 `fmt.Printf("%-N s %s\n", ...)` 平铺，不进表格
- 写新输出前先 grep 邻居命令的渲染方式再动手，避免风格漂移

## 全局标志（root PersistentFlags）
- `--profile string`（default "default"）— 凭证 profile 名，绑定全局变量 `cmd.Profile`（包级 `var Profile = "default"`，单测无需 cobra 解析也能用）
- `--meta-server-url string` — Meta Server **主机地址**（只到主机名），覆盖 profile config 与环境 preset，绑定 `cmd.MetaServerURL`；网关前缀 `/api/make` 由 `withGateway` 自动补齐
- `--repo-server-url string` — 代码仓库服务（make-repo）基础 URL，覆盖 profile config 与环境 preset，绑定 `cmd.RepoServerURL`
- `--env string` — 后端环境（dev/test/production），覆盖 `[settings] environment`，绑定 `cmd.Environment`；空串=回退 settings 或默认 production。后端 URL 三件套兜底来自 `config.Environment` preset（解析链 flag > profile config > 环境 preset，见 client.go resolveEnvironment）
- `--debug`（隐藏）— 输出 curl 调试信息，绑定 `cmd.DebugMode`

三者都不再从 `runXxx` 签名穿越，统一由 `newClientFromProfile()`（零参数）在内部直接读取全局。新增子命令时：
- 禁止声明本地 `--profile`
- `runXxx` 函数不要带 profile 参数
- 客户端构建调用 `newClientFromProfile()` 即可，不传 profile/server/env 参数；每命令横切选项（如 dry-run）经 `newClientFromProfile(api.WithDryRun(dryRun))` 变参注入
- 单测需要切换 profile 时用 `setProfile(t, "name")`（stdout_test.go），t.Cleanup 自动还原

## 成员清单
- `root.go`: 根命令入口，挂载所有顶级子命令（login / whoami / schema / apply / diff / update / skills / integration / preflight 等；deploy 已下沉为 app 子命令），对外暴露 Execute(version, date)；定义全局 PersistentFlag --profile / --meta-server-url / --repo-server-url / --env / --debug，分别绑定全局变量 Profile / MetaServerURL / RepoServerURL / Environment / DebugMode；后端 URL 兜底交给 config.Environment preset（不再持 default*Server 常量）；钩入 notifier.Start/Finish 生命周期；包内 commandName 解析顶级命令名；rootCmd.SilenceErrors=true 把错误呈现收口到 Execute 出口的 reportExecuteError（errors.go）单一出口
- `errors.go`: CLI 错误呈现单一出口：reportExecuteError(w, err) 把退出码哨兵（errDiffFound/errPreflightFailed）静默、api.ErrAuthFailed 升级为带 makecli login next-step + profile/env 回显的引导（authFailedHint）、其余复刻 cobra 的 `error: <msg>`；终结了原 diff/preflight 各自 SilenceErrors+自打印的特例
- `errors_test.go`: 覆盖 reportExecuteError 各分支（nil/退出码哨兵静默/真实错误打印/鉴权升级）+ authFailedHint 回显 profile/env，用 setProfile/setEnvFlag 隔离全局态
- `root_test.go`: 覆盖 commandName 顶级命令解析的单元测试（version/version list/update/app create/空 args/未知命令）
- `version.go`: version 子命令组，默认 Run 打印当前版本（参考 GitHub CLI 模式），挂载 list 子命令
- `version_test.go`: 覆盖 formatVersion / changelogURL 的纯函数测试
- `version_list.go`: version list 子命令，调 internal/update.ListReleases 拉取 GitHub 最近 N 条 release，tablewriter 输出 CURRENT/VERSION/TYPE/PUBLISHED/URL（TYPE 经 releaseTypeLabel 标注 Pre-release 对齐 gh release list，正式版留空；JSON 输出保留 name 与 prerelease 字段）；CURRENT 列对比 build.Version 标记当前安装版本；支持 --limit（默认20，1-100）/ --output（table|json）
- `version_list_test.go`: 覆盖 runVersionList 的单元测试（table 渲染 / CURRENT 标记 / TYPE 列 Pre-release 标注 / JSON 输出去除 assets / 空列表 / 非法 limit / 非法 output / API 错误 / DEV 不打标记），用 httptest 隔离网络
- `configure.go`: configure 命令组（复用全局 --profile + 本地 --sample；组级 PersistentPreRunE 经 config.ValidateProfileName 拒绝保留名 settings 作 profile），默认行为等同 token 子命令，--sample 打印带注释 INI 参考模板到 stdout 即退（sampleConfig 常量：profile 覆盖键以占位值平铺展示当参考卡，直写需替换占位主机名或删行回退 preset；configure_test.go 完整性测试守护其含全部 validConfigKeys 且活跃值过真实 loader）；子命令: token（交互写 ~/.make/credentials）/ config（交互写 ~/.make/config）/ set（直接写单个 key，Long+Example 摊开 environment/channel 魔法路由与合法 key）/ get（读取单个 key，Long+Example 对称）/ verify（在线验证 token）；validateConfigKey 校验合法 profile key 集合（meta-server-url / repo-server-url / auth-server-url / X-Tenant-ID / X-Operator-ID）；特殊键 environment 经 setEnvironment 路由到全局 [settings]（非 profile，值校验 ∈ dev/test/production，写 config.SetSetting），get environment 读 settings 缺省回退 production；特殊键 channel 经 setChannel 同款路由（值校验 ∈ stable/beta），get channel 缺省回退 stable
- `login.go`: login 顶级命令，浏览器 OAuth 登陆（复用全局 --profile，本地 --timeout 默认3m / --no-open-browser）；runLogin 前置 config.ValidateProfileName（拒绝保留名 settings，免做完 OAuth 才在落盘时报错），再编排 discover→起动态端口回调 server→每次新注册 client（RFC 7591）→PKCE→开浏览器→等回调→换 token，仅把 access_token 写入 ~/.make/credentials[Profile]；make preset（business_type=make / scopes=make:resources）；OAuth 元数据地址由身份服务器基址拼 .well-known 路径（authMetadataURL），基址取 profile 的 auth-server-url、缺省回退当前环境 preset 的 AuthServerURL（resolveEnvironment），使 token 颁发方与后端环境对齐；openBrowserFunc 包级可打桩变量；defaultLoginTimeout=3m 为 --timeout 缺省值，被 whoami 自动登录复用
- `login_test.go`: 覆盖 runLogin 的单元测试（成功写 token / 写入选定 profile / --no-open-browser 打印 URL 并超时 / 缺 registration_endpoint 报错），用 httptest 模拟 OAuth 三端点 + openBrowserFunc 桩注入回调，t.Setenv 隔离凭证
- `whoami.go`: whoami 顶级命令，展示当前 token 对应的用户身份（调 api.GetUserInfo，走网关 GET /user/v1/info）；交互对齐 wrangler whoami——登录状态收敛为「每次调用至多触发一次登录」：无 token 先 login 再查；有 token 直查，鉴权失败（ErrAuthFailed，过期/失效）login 后重试一次，仍失败原样上抛（errors.go 升级为引导文案）；渲染 wrangler 式问候语 + tablewriter FIELD/VALUE 表格（User ID/Name/Tenant ID/Tenant/Profile/Environment，对齐 app list 表格约定），valid=false 时 stderr 警示；支持 --output table|json（默认 table）；loginFunc 包级可打桩变量隔离 OAuth 流程；前置 validateOutputFormat + config.ValidateProfileName（拒绝保留名 settings）
- `whoami_test.go`: 覆盖 runWhoami 的单元测试（有 token 直接展示不触发登录 / 无 token 触发登录后展示 / 过期 token 重登录并重试一次 / 重登录后仍鉴权失败原样上抛不死循环 / 登录失败透传 / valid=false stderr 警示 / JSON 输出 / 非法格式 / 保留名 profile 拒绝），stubLogin 打桩 loginFunc 隔离 OAuth + newUserInfoServer 按 token 回放身份或 401 + httptest 隔离网络；测试数据已脱敏
- `configure_test.go`: 覆盖 mask / validateJWT / validateConfigKey 的纯函数测试 + configure set/get environment（写读全局 [settings]、非法环境名拒绝、不受 --profile 影响）+ set/get channel（写读、缺省回退 stable、非法通道名拒绝）+ sampleConfig 完整性守护（含 channel 键与活跃值过真实 loader）
- `configure_verify.go`: configure verify 子命令，加载 credentials + config，JWT 格式校验后调 ListApps 在线验证 token；输出 verifyResult（profile/valid/token/meta_server_url/tenant_id/operator_id/message）；支持 --output table|json；valid=false 时 exit 1
- `configure_verify_test.go`: 覆盖 runConfigureVerify 的单元测试（valid token table/json、token not configured、malformed JWT、server 401、config 字段传递、unknown profile），用 httptest 隔离网络
- `configure_resolve.go`: configure resolve 子命令，token-free 离线解析——不联网、不校验 token，只把当前 profile / environment / override 解析成本地预览工具所需的最小 JSON（configureResolveResult{profile, environment, make_api_origin, tenant_id, operator_id}）；--target local-preview（默认，唯一合法值）/ --output json（默认，唯一合法值），非法值拒绝；make_api_origin 取值链 flag > profile config > 环境 preset（firstNonEmpty(MetaServerURL, cp.MetaServerURL, env.MetaServerURL)），经 normalizeMakeAPIOrigin 裁掉 apiGatewayPath 网关前缀与尾斜杠回归纯 origin；resolveEnvironmentForConfigureResolve 复用 --env>settings>默认解析链但返回环境名，未知名报错
- `configure_resolve_test.go`: 覆盖 runConfigureResolve 的单元测试（production/test/dev 三环境 origin 解析 + JSON 输出含 make_api_origin / profile config 覆盖 preset / flag 覆盖 profile / 已带网关前缀的 origin 归一化裁剪 / 非法 target 与 output 拒绝），setMetaServerURL 临时覆盖全局 MetaServerURL、t.Setenv 隔离凭证与环境
- `client.go`: 公共 helper，resolveProfile() 收口凭证与配置解析（前置 config.ValidateProfileName 拒绝保留名 settings），resolveEnvironment() 收口环境 preset（--env flag > [settings] environment > 默认 production，未知名报错），envName() 是其 fail-safe 姊妹（同解析链但吞 LoadSettings 错误回退默认，供错误展示回显环境名），resolveChannel() 收口发布通道解析（[settings] channel > DefaultChannel=stable，未知名报错，被 update 命令消费；notifier 侧静默回退是 channelOf 的职责）；newClientFromProfile(...api.Option)（收变参横切选项，如 WithDryRun）构建 Meta/Data 客户端，newRepoClientFromProfile() 构建代码仓库服务客户端并额外返回裸 token（供 deploy 的 git Basic 认证）；firstNonEmpty 统一「flag > profile config > 环境 preset」取值链；withGateway 把解析出的主机基址补上网关前缀 apiGatewayPath=/api/make（幂等：完整 URL 原样返回），Meta/Repo 客户端构造与 configure verify 共用
- `client_test.go`: 覆盖 resolveEnvironment 优先级（默认 production / settings 覆盖默认 / --env 覆盖 settings / 未知环境报错）、resolveChannel（缺省回退 stable / settings 读 beta / 未知值拒绝）与 withGateway 网关前缀拼接（补前缀/幂等/裁尾斜杠/空串），setEnvFlag 临时覆盖全局 Environment
- `stdout_test.go`: 测试基础设施，提供 captureStdout 劫持 stdout 的辅助函数 + setProfile(t, name) 临时覆盖全局 Profile（t.Cleanup 还原）
- `app.go`: app 命令组，挂载 create / init / list / delete / deploy 子命令；提供 loadAppManifestFromFile 共享 helper（从 YAML 加载唯一 Make.App 资源）
- `git.go`: cmd 模块共享 go-git 原语层（与具体命令无关），被 app_init / app_create / deploy 复用：openRepo（只开不 init，缺失即指引 app init，DetectDotGit 向上探测）、initGitRepo（PlainOpen 不探测父仓库→该目录自身建 .git，幂等）、ensureGitignore（增量补齐，agents/gitignore.tmpl 做期望条目单一真相源，缺则追加 `# added by makecli`，文件不存在写模板全文）、gitSignature（user.name/email 缺省回退 makecli；用 LocalScope 合并 system+global+local，修正原 SystemScope 读不到全局 gitconfig 的 bug）、stageAndCommit（add -A 尊重 .gitignore，干净则不造空提交）、assertDeployable（有 HEAD + 工作树干净，否则可操作报错）；命令专属 git 约定（部署分支/匿名 remote/push）仍内聚 deploy.go
- `git_test.go`: 覆盖 git.go 共享原语的单元测试（建仓幂等 / .gitignore 写全+增量补齐+完整不重写 / stageAndCommit 提交+空树no-op+尊重gitignore / assertDeployable 无HEAD拒绝+干净通过+脏树拒绝 / openRepo 缺失指引 app init / gitSignature 回退 makecli + LocalScope 读本地配置回归守护），t.TempDir 隔离 FS + 真实 go-git
- `app_create.go`: app create 子命令——= app init 本地脚手架内核 + 远端 App 注册 + initial commit + 代码仓库。位置参数 <appKey> 同时是「目录名 + key」（deriveAppKey 取 filepath.Base(filepath.Abs(arg)) 推导，覆盖 shop/`.`/`..`，validResourceKey 把关）；--name 展示名（缺省=key），--description；执行序「远端先行」 解析key→加载凭证→CreateApp(远端,先于任何本地写)→writeScaffold(幂等 skip-if-exists 写 CLAUDE.md/AGENTS.md/apps/dsl/app.yaml,返回新建清单;scaffoldFile{embed,out} 映射模板名;scaffoldOutputs 收口顺序;fileExists 只读判定)→scaffoldGit(initGitRepo+ensureGitignore+stageAndCommit「Initial scaffold for <key>」→create 产物即干净可立即 deploy;git 失败降级 stderr 警告不阻断已成功的远端创建,全程不写 stdout 保持成功输出一行)→prepareCodeRepos——新目录远端失败(坏token/冲突/网络)时本地零残留,换 profile 重跑干净；writeScaffold 幂等故去掉旧 assertScaffoldClear 硬拒，create 可与 init 组合(先 init 本地再 create 补远端,不覆盖用户编辑)；prepareCodeRepos 成功静默（成功只打印 `App 'X' created successfully` 一行），失败降级为 stderr 警告（deploy 幂等重试）；newAppManifest 单一构造点（脚手架与 CreateApp 共用,被 app_init 复用,空 description 不进 properties 避免 diff 漂移）；renderAppDSL 用 ResourceManifest+yaml.Marshal(2空格缩进) 写出可被 apply/diff 读回的 app.yaml；-f 文件模式仅建远端不脚手架；--dry-run 经 api.WithDryRun 注入 X-Dry-Run 让远端校验不落库，校验通过即收尾（跳过脚手架/git/仓库准备等真实副作用），脚手架与文件两模式同款短路
- `app_create_test.go`: 覆盖 renderAppDSL / writeScaffold / runAppCreate / runAppCreateFromFile 的单元测试（DSL渲染+往返/脚手架文件不含.gitignore且报新建清单/AGENTS.md 关键合同经 assertGeneratedAgentsContract 校验/幂等skip-if-exists保留用户编辑/scaffolds 干净 git 仓库带 initial commit 即可 deploy/appKey推导含'.'/成功输出简洁无仓库信息/仓库失败 stderr 警告但 create 成功/API失败本地零残留/与预先 init 脚手架组合不硬拒且保留编辑/无凭证不留半成品/未知profile/文件模式/非法目录名拒绝），用 httptest 隔离网络 + t.TempDir 隔离 FS + chdir helper 测 '.' 推导 + captureStderr 测降级警告；stubRepoServer 临时指向仓库 mock；含 validResourceKey 通用 key 校验测试（长度 2-20，不可以下划线开头）
- `app_init.go`: app init 子命令——把目录变成完整本地 Make app 项目（与 create 共享同一脚手架内核，create = init 核心 + 远端注册 + initial commit）：runAppInit 复用 deriveAppKey/newAppManifest/writeScaffold（app_create）+ initGitRepo/ensureGitignore（git.go）写 CLAUDE.md/AGENTS.md/apps/dsl/app.yaml + git init + .gitignore，逐项 statusWord 打印 created/exists·initialized/already·updated/complete 状态行；可选 [appKey]（兼目录名，deriveAppKey 推导，缺省取 cwd 名，validResourceKey 把关），全程幂等 skip-if-exists，刻意不 commit（提交时机交还用户）、不碰远端；deploy 的 openRepo 门控依赖此命令先行建仓
- `app_init_test.go`: 覆盖 runAppInit 的单元测试（完整脚手架 CLAUDE.md/AGENTS.md/app.yaml/.gitignore/.git + AGENTS.md 关键合同经 assertGeneratedAgentsContract 校验 / app.yaml key 取目录名 / 缺省 appKey 取 cwd 名 / 非法目录名拒绝 / 幂等再跑报 exists·already·complete 且保留用户编辑 / 刻意不 commit→refs/heads 为空无分支），t.TempDir 隔离 FS + chdir 测默认 cwd + captureStdout 验证状态行
- `agents_contract_test.go`: app init / app create 共享的 AGENTS.md 合同断言，集中校验 App Contract、统一登录、Service 代理、运行时产物、发布前验证和旧标记禁用，避免两个测试类重复锁定大量自然语言文案
- `app_list.go`: app list 子命令，调用 MakeService.ListResources 分页列出 org 下全部 App，输出列 KEY/NAME/DESCRIPTION/VERSION/CREATED AT（description 取自 app.Properties["description"]，空则留空格）；支持 --profile / --server / --page / --size / --filter flags；parseFilter 把 "key=value" 过滤语法翻译为 CEL 表达式文本（key 走等值 `key == 'v'`，name/description 走 `field.contains('v')`，逗号 = `||`），celString 转义单引号字面量；服务端按 Expression{expression} 解析（见 AgenticDSL/Design/ExpressionDesign.md）
- `app_list_test.go`: 覆盖 runAppList / parseFilter 的单元测试（成功/空列表/分页JSON/过滤请求/非法过滤/无凭证/API错误/非法页码），用 httptest 隔离网络
- `app_delete.go`: app delete 子命令，调用 Meta Server API（MakeService.DeleteResource）删除指定 App；删除前要求原样输入 app key 确认（gh repo delete 同款强护栏，charm.land/huh/v2 表单 + Validate 阻断错值提交、ErrUserAborted 视为取消；非交互终端经 go-isatty 检测直接拒绝并指引 --yes，杜绝挂起），--yes/-y 跳过确认；confirmDeleteFunc 包级可打桩变量隔离终端交互；支持 -f YAML 文件模式
- `app_delete_test.go`: 覆盖 runAppDelete / runAppDeleteFromFile 的单元测试（--yes 删除 / 确认通过后删除 / 确认拒绝在触网前短路 / 非交互真门控拒绝 / 无凭证 / API错误 / 未知profile / 文件模式），stubConfirm 打桩 confirmDeleteFunc 隔离终端交互 + httptest 隔离网络
- `entity.go`: entity 命令组，挂载 create / delete / list 子命令；--app（appKey）参数为子命令继承
- `entity_create.go`: entity create 子命令，位置参数为 Entity key，--name 为展示名（缺省回退 key），--json 加载整个 properties（fields + uniqueConstraints，同形于 DSL 的 properties 与 entity list -o json 的 data.properties，三者可往返）；校验 field.Key 合法（validResourceKey）+ 约束字段引用（validateConstraintFieldRefs：name 的引用字段须在已声明 fields 内、≥1 字段；类型白名单/配额/存量重复交服务端裁决）；--app 传 appKey；--dry-run 经 api.WithDryRun 注入 X-Dry-Run 远端校验不落库，成功打印 would-be 行；loadEntityProperties 从 JSON 文件加载 api.EntityProperties 透传
- `entity_create_test.go`: 覆盖 runEntityCreate / validateConstraintFieldRefs 的单元测试（成功/带fields/field key 校验/无凭证/API错误/未知profile/非法JSON + --json properties 的 uniqueConstraints 到达线缆/未声明字段拒绝/字段引用校验），用 httptest 隔离网络
- `entity_delete.go`: entity delete 子命令，按 key 调用 Meta Server API（MakeService.DeleteResource）删除指定 Entity；--app 传 appKey
- `entity_delete_test.go`: 覆盖 runEntityDelete 的单元测试（成功/无凭证/API错误/未知profile），用 httptest 隔离网络
- `entity_list.go`: entity list 子命令，按 appKey 分页列出 entity（KEY/NAME/VERSION），位置参数为 entity key 时显示详情（Key/Name/App/Version + fields 表格 KEY/NAME/TYPE + 唯一性约束表 NAME/FIELDS，renderUniqueConstraints 无约束则静默）；--app 传 appKey；复用 parseFilter
- `entity_list_test.go`: 覆盖 runEntityList 的单元测试（列表/空列表/过滤请求/具体entity/无字段/唯一性约束表/无凭证/API错误/未知profile），用 httptest 隔离网络
- `relation.go`: relation 命令组，挂载 create / update / delete / list 子命令；--app（appKey）参数为子命令继承
- `relation_create.go`: relation create 子命令，位置参数为 Relation key，--name 为展示名，--json 加载 from/to（entityKey 引用），调用 Meta Server API 创建 Relation；--dry-run 经 api.WithDryRun 注入 X-Dry-Run 远端校验不落库，成功打印 would-be 行
- `relation_create_test.go`: 覆盖 runRelationCreate 的单元测试（成功/无凭证/API错误/未知profile/非法JSON/文件不存在），用 httptest 隔离网络
- `relation_update.go`: relation update 子命令，按 key 定位，--name 更新展示名，--json 加载 from/to（entityKey 引用），调用 Meta Server API 更新 Relation
- `relation_update_test.go`: 覆盖 runRelationUpdate 的单元测试（成功/无凭证/API错误/未知profile/非法JSON），用 httptest 隔离网络
- `relation_delete.go`: relation delete 子命令，按 key 调用 Meta Server API 删除指定 Relation
- `relation_delete_test.go`: 覆盖 runRelationDelete 的单元测试（成功/无凭证/API错误/未知profile），用 httptest 隔离网络
- `relation_list.go`: relation list 子命令，按 appKey 分页列出 relation（KEY/NAME/FROM/TO/VERSION，FROM/TO 显示 entityKey(cardinality)），位置参数为 relation key 时显示详情；复用 parseFilter
- `relation_list_test.go`: 覆盖 runRelationList 的单元测试（列表/空列表/JSON列表/详情/JSON详情/过滤请求/无凭证/API错误/未知profile/非法页码/非法格式），用 httptest 隔离网络
- `record.go`: record 命令组，挂载 create / get / update / delete / list 子命令；--app（appKey）和 --entity（entityKey）参数为子命令继承
- `record_create.go`: record create 子命令，从 JSON 文件加载动态 KV 数据，调用 Data Service API 创建 Record，输出 recordID；loadRecordData 从 JSON 文件加载记录数据；支持 --app（继承）/ --entity（继承）/ --json（必选）/ --dry-run（经 api.WithDryRun 注入 X-Dry-Run 远端校验不落库，成功打印 would-be 行、不展示回滚事务 recordID）/ --profile
- `record_create_test.go`: 覆盖 runRecordCreate 的单元测试（成功/无凭证/API错误/未知profile/非法JSON/文件不存在），用 httptest 隔离网络
- `record_get.go`: record get 子命令，获取单条 Record 并按 key 排序逐行输出或 JSON 格式展示；支持 --app（继承）/ --entity（继承）/ --profile / --output
- `record_get_test.go`: 覆盖 runRecordGet 的单元测试（成功/JSON输出/无凭证/API错误/未知profile/非法格式），用 httptest 隔离网络
- `record_update.go`: record update 子命令，透明路由——1 个 recordID 走 /data/v1/record 单条更新，N 个走 /data/v1/field 批量更新；支持 --app（继承）/ --entity（继承）/ --json（必选）/ --profile
- `record_update_test.go`: 覆盖 runRecordUpdate 的单元测试（单条路由验证/批量路由验证/无凭证/API错误/未知profile/非法JSON），用 httptest 隔离网络，重点验证请求路径
- `record_delete.go`: record delete 子命令，批量删除 Record，汇报每条记录的删除结果；支持 --app（继承）/ --entity（继承）/ --profile
- `record_delete_test.go`: 覆盖 runRecordDelete 的单元测试（单条/批量/部分失败/无凭证/API错误/未知profile），用 httptest 隔离网络
- `record_list.go`: record list 子命令，分页查询 Record，自动从首条记录提取列名或使用 --fields 指定列，parseSortSpec 解析排序说明，extractKeys 提取 map 键；--filter 直收 raw CEL 表达式（不在 CLI 解析，原样塞进 filter.expression，服务端裁决合法性；命令带 Long+EXAMPLES 教 CEL 用法）；支持 --app（继承）/ --entity（继承）/ --profile / --page / --size / --output / --fields / --sort / --filter
- `record_list_test.go`: 覆盖 runRecordList 的单元测试（表格/JSON/空列表/无凭证/API错误/未知profile/非法页码/非法格式/非法排序），用 httptest 隔离网络
- `deploy.go`: app deploy 子命令（`makecli app deploy`）——「纯 push 已提交状态」（提交时机交还用户，不再自动 add/commit）。--env preview|production（默认 preview，production 需显式 opt-in）；production push 前过 continue/abort 确认（charm.land/huh confirm + go-isatty，confirmDeployFunc 包级可打桩，--yes/-y 跳过，非交互终端拒绝并指引 --yes，与 app delete 同款护栏），preview 不拦；app key 取自 apps/dsl/app.yaml（appKeyFromDSL：app 身份单一真相源，文件缺失即给可操作错误，无 --app 旋钮）；执行序本地门控先行(fail-fast,网络之前)：openRepo(git.go,缺 .git 即报错指引 app init)→assertDeployable(git.go,无 commit/工作树脏即报错让用户先 commit)→assertAppRegistered(newClientFromProfile+Meta GetApp 把关 app 已注册：ErrNotFound 即指引 `makecli app create -f apps/dsl/app.yaml` 避免「有仓库、无 app」孤儿状态，其余错误原样上抛不放行；在建仓库/推送之前短路)→打印 App/Environment→production 确认门控→newRepoClientFromProfile→CreateRepository(MakeService.CreateResource 幂等)取 cloneUrl→pushCurrentHead；pushHead 用 go-git/v5(纯 Go,二进制自包含)：CreateRemoteAnonymous 承载 cloneUrl 不落 .git/config，推 head→refs/heads/<deployBranch> 常量 dev webhook 约定，HTTP BasicAuth make:<token>，NoErrAlreadyUpToDate 视为成功；--force 透传 PushOptions.Force；gitPushFunc 包级变量便于测试打桩（仅隔离推送，本地仓库门控跑真 go-git）；建仓+.gitignore+commit 已下沉 app init / app create（git.go）
- `deploy_test.go`: 覆盖 runDeploy 编排（pushCall 打桩 gitPushFunc 隔离推送，gitCommitAll 建真 committed 仓库过门控：preview/production 目标与 --force 透传、app.yaml 取 key、app.yaml 缺失/key 非法、非法 env、fail-fast 无.git/无commit/脏树报错且 noNetRepoServer 证不触网、app 未注册指引 app create 且短路不触仓库、注册校验错误不误判为「不存在」、无凭证、API 错误、cloneUrl 缺失、push 错误透传）+ pushCurrentHead 真 go-git（newBareRemote 本地裸仓库做 file-transport push 目标：推已提交 HEAD 到 dev 分支、干净 redeploy 为 up-to-date no-op、新提交后推更新 dev），enterAppDir 临时工程目录隔离 FS + httptest 隔离网络（newAppExistsMeta 放行注册门控 / stubMetaServer 临时指向 Meta）
- `preflight.go`: preflight 顶级子命令，以 make-build-service build_spec.md 第 5 节检查清单为实现依据（可选位置参数 [dir]，默认 cwd；--app-type 已移除）：构建模式 A/B 自动判定（apps/ui|service/package.json 任一存在→A，否则 B）、包管理器按 lockfile 优先级判定（pnpm>yarn>npm，检测目录 A=apps/ B=根；无 lockfile 回退 npm）；buildPreflightContext 一次收集事实（各 package.json/pnpm-workspace.yaml 三态投影 pkgFile、app.yaml 单次解析得出 appKey/appYAMLReadable 事实，resolveRepoName 复用该事实取 key 回退目录名而非重新解析），preflightChecks 表驱动求值（条目与 spec 1:1：G1/G2/P1、A1-A9/A11/A15(TEMP)、B1-B3 + makecli 自有 D1=apps/dsl/ 存在且 app.yaml key 可读（目录在但 key 读不出同样报错，堵住「preflight 绿、deploy 红」缺口）；BUILD-TIME 启发式 A10/A12/A13/A14/B4 本版不做），A8 刻意不限模式（spec §7 要求孤儿组件目录回退 B 时同报 A8+B1）；ERROR/WARN/INFO 三级输出（✓/✗/!/i + 检查 ID），失败条目附 How to fix 块（每条 fix 指引：为什么+改哪+改成什么，面向 AI agent 一步收敛；A15 按 ctx.lockfiles 是否为空分支，无 lockfile 可 import 时不建议 `pnpm import`）；存在 ERROR 返回 errPreflightFailed（退出码 1），仅 WARN/INFO 放行，作 CI/deploy 前置门禁
- `preflight_test.go`: 覆盖 preflight 的单元测试（文件投影 workspacesField 双形态/pkgFile 三态/pnpm-workspace 解析、lockfile 优先级与 npm 回退、workspaceCovers glob 各形态、上下文构建含孤儿组件目录不触发模式 A、repoName 两来源、检查表全条目含 spec §7 常见失败结构 8 行与 A8+B1 同报、渲染与退出码含 How to fix 块/仅警告放行/--app-type 已移除），evalChecks 白盒跑表收集分级 id + pfModeAPnpm/pfModeAYarn/pfModeB 全绿夹具做减法 + t.TempDir 隔离文件系统；TestPreflightChecksTableInvariants 守护表不变式（ERROR/WARN 必有 fix、ERROR 必有 label）；D1 补测 app.yaml 缺失/不可读 YAML/key 为空三种「目录在但 key 读不出」场景；TestA15FixHint 锁定 fix 按 ctx.lockfiles 分支（有 lockfile 建议 pnpm import，无则只建议 pnpm install）
- `apply.go`: apply 子命令，从 YAML 文件/目录批量应用资源（create-or-update 语义：按 Key 检测存在性，App 不存在则创建/已存在则跳过，Entity/Relation 不存在则创建/已存在则更新）；存在性判定经 api.ErrNotFound 哨兵——仅"确实不存在"才创建，Get 的瞬时/传输/非 not-found 错误一律上抛不创建（杜绝误建重复资源或把 update 降级为 create）；依赖顺序 App→Entity→Relation；ResourceManifest 提供 Key（标识符）/Name（展示名）/Type/AppKey/Meta/Properties 字段；applyEntity 经 extractUniqueConstraints 从 properties.uniqueConstraints 结构性透传唯一性约束（合法性交服务端）→构建 api.EntityProperties 写入；支持多文档 YAML；目录扫描经 loadManifestsFromDir（filepath.WalkDir）递归，--max-depth 控深度（1=当前目录/2=含子目录默认/0=不限，负值拒绝），隐藏文件与隐藏子目录（.git 等）跳过，subdirLevel 算相对根层级做剪枝；loadManifestsFromDir/subdirLevel 被 diff 复用
- `apply_test.go`: apply 子命令的单元测试，覆盖单文件、多文档、目录扫描、Relation 创建/更新/缺 appKey 字段错误、App+Entity+Relation 混合目录场景、uniqueConstraints 到达线缆 + extractUniqueConstraints 解析/错误
- `diff.go`: diff 子命令，对比远端 Meta Server 上的 App DSL（Entity + Relation + 唯一性约束）与本地 YAML 文件的差异；App key 从 YAML 自动推断（Make.App key 或 Entity/Relation appKey 字段）；分页获取全部远端资源，按 Key 匹配后逐字段（key）/端点（entityKey）/唯一性约束（compareConstraints 按 name 匹配、fields 顺序敏感 slices.Equal）比对，约束差异并入 EntityDiff.Constraints 且影响 entity changed 判定；支持 -f（必选）/ --output / --max-depth（与 apply 同款，默认 2，0=不限，复用 loadManifestsFromDir，保证 diff/apply 目录展开一致）；退出码 0=无差异 1=有差异（table 与 json 模式一致，经 errDiffFound 哨兵上抛到 main 转译，不再用 os.Exit，可单测）；真实错误与 errDiffFound 哨兵均交由 reportExecuteError（errors.go 单一出口）处理——哨兵静默、真实错误打印
- `diff_test.go`: 覆盖 diff 子命令核心逻辑的单元测试（computeDiff/computeRelationDiff/computeDiffUniqueConstraints/fetchAllEntities/fetchAllRelations/jsonDeepEqual/runDiff 错误路径），用 httptest 隔离网络
- `schema.go`: schema 顶级子命令，按 appKey 调用 MakeService.GetResource 获取聚合 Schema（App + Entities + Relations），JSON 输出
- `schema_test.go`: 覆盖 runSchema 的单元测试（成功/无凭证/API错误/未知profile），用 httptest 隔离网络
- `output.go`: list 命令通用输出辅助（table|json 格式校验 + JSON 编码），被 app list / entity list / relation list / record list / record get 复用
- `skills.go`: skills 命令组，挂载 list / update / remove 子命令；默认 RunE = list（version.go 同款 gh 模式），组级 --output 供裸 `makecli skills` 使用
- `skills_list.go`: skills list 子命令，调 skillsync.List 合并本地 lockfile 与 GitHub 远端状态，tablewriter 输出 NAME/STATUS/DESCRIPTION(60 rune 截断)/UPDATED AT(裁到日期)；汇总行 N installed, M outdated, K available + update 提示；LockWarning/RemoteErr 渲染为 stderr 警告不阻断，退出码恒 0；支持 --output table|json（JSON 保留全文 description 与完整时间戳）；listSkillsFunc 包级可打桩变量
- `skills_list_test.go`: 覆盖 runSkillsList 的单元测试（table 渲染+汇总行/JSON 全文/空态引导/警告进 stderr 不阻断/非法格式/裸 skills 默认= list/truncateLine），stubListSkills 打桩隔离 lockfile 与网络
- `skills_update.go`: skills update 子命令，复用 update.go 的 runSkillSync（skillsync.Sync 幂等：装缺的 + 升级已有的），与 makecli update 后置同步同一代码路径
- `skills_update_test.go`: 覆盖 skills update 走 runSkillSync 且 Skip 恒 false，syncSkillsFunc 打桩避免真实执行 npx
- `skills_remove.go`: skills remove 子命令，名字必填（MinimumNArgs(1)），透传 skillsync.Remove（来源校验挡住误删第三方 skills）；removeSkillsFunc 包级可打桩变量
- `skills_remove_test.go`: 覆盖 skills remove 的透传/错误上抛/必填参数，stubRemoveSkills 打桩隔离 npx
- `update.go`: update 子命令，支持 [version] 位置参数（v0.2.0 或 0.2.0）、--force、--check、--skip-skills；无 arg 走 CheckLatest 流程（经 resolveChannel 按 [settings] channel 选通道：stable 现状、beta 走列表取 semver 最高，输出经 channelSuffix 回显 [beta channel]），指定版本走 GetRelease（beta tag 天然可装，显式 opt-in 路径）；CompareVersions 决定 upgrade/same/downgrade 分支，降级需 --force；DEV 版本跳过比较；stable 通道下持有高于最新稳定版的预发布版本时 hintBetaAboveStable 补降级去向提示（--force 回稳定或等更高稳定版）；--check 走 runUpdateCheck 仅检查不安装、不同步 skills（有更新打印 current→tag + Release HTMLURL + changelogFileURL 的 CHANGELOG.md 链接 + install 提示，已最新打印 Already up to date），--check 与版本参数互斥；二进制流程结束后默认每次调用 internal/skillsync 同步 Make platform skills，并保留原 binary 输出后追加 skills 输出；applyFunc / syncSkillsFunc 包级变量便于测试打桩
- `update_test.go`: 覆盖 runUpdate / runUpdateCheck 的单元测试（latest 已到位/有更新、specific 升级/同版本/降级拒绝/--force 降级/规范化无 v 前缀/非法 semver/tag 不存在/DEV 跳过比较、skills 输出、--skip-skills 透传；--check 有更新报告/已最新/拒绝带版本参数且不触发 apply/skills；beta 通道走 /releases 列表端点 + [beta channel] 回显、stable 通道持 beta 版本补降级提示），applyFunc / syncSkillsFunc 打桩避免真实替换二进制或执行 npx，cobra SetOut 捕获输出，$MAKE_CLI_CONFIG_DIR 隔离通道配置
- `daemon.go`: daemon 顶级子命令（**Hidden**，功能未稳定不对普通用户展示）——以设备模式接入 Agent 平台：探测本机 coding CLI（claude-code / codex）后注册设备，心跳 + claim 轮询驱动执行（编排在 internal/daemon）；flags --gateway-url / --token / --name / --work-dir / --max-run-duration，token 缺省读 MAKE_AGENT_DEVICE_TOKEN；gateway 地址取值链 --gateway-url > MAKE_AGENT_GATEWAY_URL > 环境 preset AgentGatewayURL（随全局 --env，resolveAgentGatewayURL 收口）
- `daemon_test.go`: 覆盖 resolveAgentGatewayURL 取值链（三环境 preset / env var 覆盖 / flag 最高优先），setEnvFlag + MAKE_CLI_CONFIG_DIR 隔离全局态
- `integration.go`: integration 命令组，挂载 ocr 子命令；预留扩展点供未来其它 integration（translate / asr / embed 等）
- `integration_ocr.go`: integration ocr 子命令，校验文件后缀（.pdf/.ofd/.png/.jpg/.jpeg）后通过 newClientFromProfile 上传，调用 api.OCR；renderOCRTable 风格对齐 entity list <name>：顶部 File/Bills/Took 头部 + 每张票一个 tablewriter LABEL/VALUE 边框表格，按 spec sample 解析 result.pages[].bills[].items[]（过滤空值），断言失败回退 JSON；支持 -f|--file（必选）/ --profile / --output（table|json）/ --business-id / --verify-vat（默认 true，仅 Changed 时显式发送）/ --coord-restore-original / --pages / --crop-complete / --crop-value / --merge-elec / --return-ppi
- `integration_ocr_test.go`: 覆盖 runIntegrationOCR / renderOCRTable 的单元测试（table 渲染 spec sample / json 输出 / 非法扩展名 / 非法格式 / 文件不存在 / 无凭证 / 未知 profile / API 错误 / 异常结构回退），用 httptest 隔离网络

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
