# cmd/
> L2 | 父级: /CLAUDE.md

## 渲染约定
- 表格统一用 `github.com/olekukonko/tablewriter`（边框 + Header），禁止用 stdlib `text/tabwriter`
- key-value 头部信息（File / Name / App 等）用 `fmt.Printf("%-N s %s\n", ...)` 平铺，不进表格
- 写新输出前先 grep 邻居命令的渲染方式再动手，避免风格漂移

## 全局标志（root PersistentFlags）
- `--profile string`（default "default"）— 凭证 profile 名，绑定全局变量 `cmd.Profile`（包级 `var Profile = "default"`，单测无需 cobra 解析也能用）
- `--access-token string` / `-t` — access token 覆盖，绑定 `cmd.AccessToken`；取值链 **flag > `$MAKE_ACCESS_TOKEN` > credentials[profile].access_token**（三者同名，收口在 client.go resolveAccessToken，resolveProfile / configure verify / whoami 共用）。覆盖只替换凭证本身，tenant / operator / URL 仍取自 `--profile` 的 config 段；覆盖态下 login 写文件不会生效，故 whoami 不再自动重登录、configure verify 与鉴权引导回显 token 来源（flag/env/credentials）。flag 值会留在 shell history / ps，CI 用环境变量
- `--meta-server-url string` — Meta Server **主机地址**（只到主机名），绑定 `cmd.MetaServerURL`；取值链 **flag > `$MAKE_META_SERVER_URL` > profile config meta-server-url > 环境内置地址**（收口 client.go metaServerURL，最后一级是默认值不是配置项）；网关前缀 `/api/make` 由 `withGateway` 自动补齐
- 代码仓库服务（make-repo）主机**无 flag 级**——它是部署实现细节，不值得占全局 flag；取值链 **`$MAKE_REPO_SERVER_URL` > profile config repo-server-url > 环境内置地址**（收口 client.go repoServerURL，env 级留给 CI/测试覆盖）
- 两条覆盖类 flag（access-token / meta-server-url）可配置来源同构：flag、`$MAKE_<KEY>` 环境变量、profile 文件，help 文案统一为 `<noun> (overrides $ENV and the profile <file>)`
- `--context string` — 后端 context（dev/test/production），绑定 `cmd.Context`；取值链 **flag > `$MAKE_CLI_CONTEXT`（常量 EnvContext，前缀 MAKE_CLI_ 因选后端是工具自身行为）> 当前 profile.context > `[settings] context` > 默认 production**（收口 client.go resolveContext，login / trace / daemon / configure resolve|verify / context show 共用）；配置文件仍是旧键 `[settings] environment` 时解析链**报错指引 `makecli doctor --fix`**（doctorFixHint 常量，与 doctor 自身的 fixable 提示同文），不静默回退（旧配置写着 dev 却落到 production 是最坏兼容）。后端 URL 五件套兜底来自 `config.Context` preset。**词汇约定**：context = 连哪套 Make 后端；environment 只指 app 的部署环境（beta/production，`app deploy` / `app delete` 的本地 `--env`）——两词各占一层，全局 flag 改名 `--context` 也消除了与本地 `--env` 的同名遮蔽
- `--debug`（隐藏）— 输出 curl 调试信息，绑定 `cmd.DebugMode`

三者都不再从 `runXxx` 签名穿越，统一由 `newClientFromProfile()`（零参数）在内部直接读取全局。新增子命令时：
- 禁止声明本地 `--profile`
- `runXxx` 函数不要带 profile 参数
- 客户端构建调用 `newClientFromProfile()` 即可，不传 profile/server/env 参数；每命令横切选项（如 dry-run）经 `newClientFromProfile(api.WithDryRun(dryRun))` 变参注入
- 单测需要切换 profile 时用 `setProfile(t, "name")`（stdout_test.go），t.Cleanup 自动还原

## 成员清单
- `root.go`: 根命令入口，挂载所有顶级子命令（login / whoami / settings / context / doctor / schema / apply / diff / update / skills / integration / preflight 等；deploy 已下沉为 app 子命令），对外暴露 Execute(version, date)；定义全局 PersistentFlag --profile / --access-token / --meta-server-url / --context / --debug，分别绑定全局变量 Profile / AccessToken / MetaServerURL / Context / DebugMode；后端 URL 兜底交给 config.Context preset（不再持 default*Server 常量）；钩入 notifier.Start(cmdName)/Finish 生命周期（cmdName 在 Execute 前解析传入，供 skipCommands 抑制；Finish 仅在 err == nil 时调用且排在 reportExecuteError 之后，对齐 gh：提示只在成功运行的末尾、失败不提示；JSON 出口的 _notice 见 output.go）；包内 commandName 解析顶级命令名；rootCmd.SilenceErrors=true 把错误呈现收口到 Execute 出口的 reportExecuteError（errors.go）单一出口；installUsageTemplate(root) 装配 GitHub CLI 风格 usageTemplate 与 globalFlags/parentFlags 模板函数，根命令 help 尾附 `Skills setup (one-time, humans): makecli skills install --all --yes` 引导（仅根级，对齐 lark-cli）
- `errors.go`: CLI 错误呈现单一出口：reportExecuteError(w, err) 把退出码哨兵（errDiffFound/errPreflightFailed/errDoctorFailed）静默、api.ErrAuthFailed 升级为带 makecli login next-step + profile/context/token 来源回显的引导（authFailedHint，context 经 contextName 纯展示解析失败回显 unknown，来源为 flag/env 时附「login 不会覆盖它」提示）、其余复刻 cobra 的 `error: <msg>`；终结了原 diff/preflight 各自 SilenceErrors+自打印的特例；ExitCode(err) 对外收口错误→退出码翻译（0 成功 / errBuildFailed 与 errPromoteFailed→2 / errWaitTimeout→124 / 其余 1，被 main 消费），语义化退出码让 CI/agent 免解析文本判定 deploy --wait 结果
- `errors_test.go`: 覆盖 reportExecuteError 各分支（nil/退出码哨兵静默/真实错误打印/鉴权升级）+ authFailedHint 回显 profile/context/token 来源 + ExitCode 退出码映射（nil→0/构建未成功→2/等待超时→124/普通错误与 diff 哨兵→1），用 setProfile/setContextFlag 隔离全局态
- `root_test.go`: 覆盖 commandName 顶级命令解析的单元测试（version/version list/update/app create/空 args/未知命令）
- `root_help_test.go`: 覆盖 installUsageTemplate 的 help 渲染测试（根 help 以 skills 安装引导行结尾 / 子命令 help 不含该行），独立构造 root 不依赖包级 rootCmd
- `version.go`: version 子命令组，默认 Run 打印当前版本（参考 GitHub CLI 模式），挂载 list 子命令
- `version_test.go`: 覆盖 formatVersion / changelogURL 的纯函数测试
- `version_list.go`: version list 子命令，调 internal/update.ListReleases 拉取 GitHub 最近 N 条 release，tablewriter 输出 CURRENT/VERSION/TYPE/PUBLISHED/URL（TYPE 经 releaseTypeLabel 标注 Pre-release 对齐 gh release list，正式版留空；JSON 输出保留 name 与 prerelease 字段）；CURRENT 列对比 build.Version 标记当前安装版本；支持 --limit（默认20，1-100）/ --output（table|json）
- `version_list_test.go`: 覆盖 runVersionList 的单元测试（table 渲染 / CURRENT 标记 / TYPE 列 Pre-release 标注 / JSON 输出去除 assets / 空列表 / 非法 limit / 非法 output / API 错误 / DEV 不打标记），用 httptest 隔离网络
- `configure.go`: configure 命令组——**只管 profile 段**（复用全局 --profile + 本地 --sample；组级 PersistentPreRunE 经 config.ValidateProfileName 拒绝保留名 settings 作 profile），默认行为等同 token 子命令，--sample 打印带注释 INI 参考模板到 stdout 即退（sampleConfig 常量：profile 覆盖键以占位值平铺展示当参考卡，直写需替换占位主机名或删行回退 preset；configure_test.go 完整性测试守护其含全部 validConfigKeys + settingKeyNames 且活跃值过真实 loader）；子命令: token（交互写 ~/.make/credentials）/ config（交互写 ~/.make/config）/ set（写当前 profile 的单个 key）/ get（读单个 key）/ verify（在线验证 token）/ resolve；validateConfigKey 校验合法 profile key 集合（meta-server-url / repo-server-url / auth-server-url / X-Tenant-ID / X-Operator-ID）；全局 [settings] 键不在此——set/get 收到 settingKeys 里的名字时经 rejectSettingKey 报 `"x" is a global setting ... use: makecli settings set/get x` 并短路在写盘之前，不静默转发（命令面与 INI 段 1:1：configure ↔ [profile]，settings ↔ [settings]）
- `settings.go`: settings 命令组——config 文件 **[settings] 全局段**的用户面，与 INI 段 1:1（profile 段归 configure）：settingKeys 有序表是全局键的单一真相源，每键一行 settingKey{name, validate, value(Settings) 原始值, def 缺省}（现有 context ∈ ContextNames / channel ∈ ChannelNames / role ∈ RoleNames 且无缺省——未设置就是未设置，get 打空、list 显示 "-" / check-for-updates 布尔；oneOf 生成名单校验器），新增全局键只需加一行，set/get/list/doctor/configure 拒绝表自动跟上；requireSettingKey 按名查键——profile 键（validConfigKeys）误投时指路 `makecli configure set/get <key>`（与 configure.go rejectSettingKey 对称：两个命令组互相指路，谁都不静默转发），其余未知名列合法全局键；setSetting 是全局键的唯一校验 + 写入路径（键经 requireSettingKey、取值过该键 validate、通过后 config.SetSetting，不受 --profile 影响；`context use` 是它之上的语义糖）；effectiveSetting 回答生效值与来源（config/default）；子命令 set / get（打印生效值，未设置回退缺省）/ list（tablewriter KEY/VALUE/SOURCE，--output json 平铺 key/value/source）；isSettingKey / settingKeyNames 供 configure 与 help 文案
- `settings_test.go`: 覆盖 settings 命令组的单元测试（set 三键写入与非法值拒绝且不落盘 / 未知键列合法键 / 不受 --profile 影响、get 逐键缺省回退与配置值、list 表格 SOURCE 列计数与 JSON 视图、TestSettingKeysRoundTrip 守护键表 value() 与 loader 一致（漏接字段即红灯）、子命令注册），$MAKE_CLI_CONFIG_DIR 隔离
- `login.go`: login 顶级命令，浏览器 OAuth 登陆（复用全局 --profile，本地 --timeout 默认3m / --no-open-browser）；runLogin 前置 config.ValidateProfileName（拒绝保留名 settings，免做完 OAuth 才在落盘时报错），再编排 discover→起动态端口回调 server→每次新注册 client（RFC 7591）→PKCE→开浏览器→等回调→换 token，仅把 access_token 写入 ~/.make/credentials[Profile]；make preset（business_type=make / scopes=make:resources）；OAuth 元数据地址由身份服务器基址拼 .well-known 路径（authMetadataURL），基址取 profile 的 auth-server-url、缺省回退当前 context preset 的 AuthServerURL（resolveContext），使 token 颁发方与后端 context 对齐；openBrowserFunc 包级可打桩变量；defaultLoginTimeout=3m 为 --timeout 缺省值，被 whoami 自动登录复用
- `login_test.go`: 覆盖 runLogin 的单元测试（成功写 token / 写入选定 profile / --no-open-browser 打印 URL 并超时 / 缺 registration_endpoint 报错），用 httptest 模拟 OAuth 三端点 + openBrowserFunc 桩注入回调，t.Setenv 隔离凭证
- `whoami.go`: whoami 顶级命令，展示当前 token 对应的用户身份（调 api.GetUserInfo，走网关 GET /user/v1/info）；交互对齐 wrangler whoami——登录状态收敛为「每次调用至多触发一次登录」：无 token 先 login 再查；有 token 直查，鉴权失败（ErrAuthFailed，过期/失效）login 后重试一次，仍失败原样上抛（errors.go 升级为引导文案）；token 走 resolveAccessToken，来源为 flag/env 时鉴权失败**不**重登录（login 写下的文件 token 会被覆盖值遮蔽，重登录只是死循环）；渲染 wrangler 式问候语 + tablewriter FIELD/VALUE 表格（User ID/Name/Tenant ID/Tenant/Profile/Context，对齐 app list 表格约定；Context 经 contextName 回显，对标 docker version 的 Context: 行），valid=false 时 stderr 警示；支持 --output auto|table|json；loginFunc 包级可打桩变量隔离 OAuth 流程；前置 resolveOutputFormat + config.ValidateProfileName（拒绝保留名 settings）
- `whoami_test.go`: 覆盖 runWhoami 的单元测试（有 token 直接展示不触发登录 / 无 token 触发登录后展示 / 过期 token 重登录并重试一次 / 重登录后仍鉴权失败原样上抛不死循环 / $MAKE_ACCESS_TOKEN 覆盖态鉴权失败零登录调用、有效则直接展示 / 登录失败透传 / valid=false stderr 警示 / JSON 输出 / 非法格式 / 保留名 profile 拒绝），stubLogin 打桩 loginFunc 隔离 OAuth + newUserInfoServer 按 token 回放身份或 401 + httptest 隔离网络；测试数据已脱敏
- `configure_test.go`: 覆盖 mask / validateJWT / validateConfigKey 的纯函数测试 + TestConfigureRejectsSettingKeys（全部 settingKeys 误投 configure set/get 均报错指路 settings 且 [settings] 未被触碰）+ 保留名 profile 拒绝 + sampleConfig 完整性守护（含全部 profile 键与 settingKeyNames、活跃值过真实 loader、不含旧键）
- `configure_verify.go`: configure verify 子命令，token 走 resolveAccessToken（flag > env > credentials）+ 加载 config（在线验证的 server 走 metaServerURL 链），JWT 格式校验 → 本地 exp fail-closed 判定（parseJWTTimeClaims 免验签提取 iat/exp——过期与签名真伪无关；已过期不触网直接 invalid，无 exp/payload 不可解析则交给在线验证）→ 调 ListApps 在线验证 token；输出 verifyResult（profile/valid/source/token/issued_at/expires_at/meta_server_url/tenant_id/operator_id/message，source=flag|env|credentials，时间 RFC3339）；支持 --output table|json；valid=false 时 exit 1，stderr 的换 token 指引按来源分流（renewTokenHint：credentials→login，flag/env→更换或撤掉覆盖值）
- `configure_verify_test.go`: 覆盖 runConfigureVerify 的单元测试（valid token table/json、--access-token 压过 env 与文件且 source=flag、env token 的 source 与 stderr 指引、token not configured、malformed JWT、过期 JWT 本地拒绝且不触网（探测端点 200 也不放行）、issued_at/expires_at 字段输出、无 exp claim 回落在线验证、server 401、config 字段传递、unknown profile），fakeJWTWithClaims/saveTokenWithClaims 构造带 claims 测试 JWT + httptest 隔离网络
- `configure_resolve.go`: configure resolve 子命令，token-free 离线解析——不联网、不校验 token，只把当前 profile / context / override 解析成本地预览工具所需的最小 JSON（configureResolveResult{profile, context, make_api_origin, tenant_id, operator_id}；外部消费者（make-app-service skill 的 local preview）只读 make_api_origin）；--target local-preview（默认，唯一合法值）/ --output json（默认，唯一合法值），非法值拒绝；make_api_origin 取值链 flag > profile config > context preset（metaServerURL），经 normalizeMakeAPIOrigin 裁掉 apiGatewayPath 网关前缀与尾斜杠回归纯 origin；context 名与 preset 直接取自 client.go resolveContext（不再手写第二条链）
- `configure_resolve_test.go`: 覆盖 runConfigureResolve 的单元测试（production/test/dev 三 context origin 解析 + JSON 输出含 make_api_origin / profile config 覆盖 preset / flag 覆盖 profile / 已带网关前缀的 origin 归一化裁剪 / 非法 target 与 output 拒绝），setMetaServerURL 临时覆盖全局 MetaServerURL、t.Setenv 隔离凭证与配置
- `client.go`: 公共 helper，resolveAccessToken() 是 token 取值链唯一入口（--access-token > $MAKE_ACCESS_TOKEN（常量 EnvAccessToken）> credentials[profile]，返回 token + 来源 tokenSourceFlag/Env/Credentials；accessTokenSource() 是其不读盘的来源判定，供鉴权引导回显），resolveProfile() 收口凭证与配置解析（前置 config.ValidateProfileName 拒绝保留名 settings，token 走 resolveAccessToken，覆盖只换凭证不换 profile config），metaServerURL() / repoServerURL() 是主机地址取值链唯一入口（meta：flag > $MAKE_META_SERVER_URL > profile config > context 内置地址；repo 同构但无 flag 级，$MAKE_REPO_SERVER_URL 起；configure resolve / verify 同用），resolveContext() 是后端 context 解析链唯一入口（返回 name + preset：--context flag > $MAKE_CLI_CONTEXT（常量 EnvContext）> profile.context > [settings] context > 默认 production；[settings] 仍有旧键（Settings.Legacy 非空）时报 "config is outdated ... run: makecli doctor --fix"（doctorFixHint），flag/env/profile context 指定则不读 settings 故绕过守卫；未知名报错），contextName() 是其纯展示姊妹（解析失败回显 "unknown" 而非谎报，供鉴权引导与 whoami），resolveChannel() 收口发布通道解析（[settings] channel > DefaultChannel=stable，未知名报错，被 update 命令消费；notifier 侧静默回退是 channelOf 的职责）；resolveRole() 同款收口角色解析（[settings] role，未设置返回空串 = 原有全量行为，未知名报错，被 update 后置同步与 skills list 消费）；newClientFromProfile(...api.Option)（收变参横切选项，如 WithDryRun）构建 Meta/Data 客户端，newRepoClientFromProfile() 构建代码仓库服务客户端并额外返回裸 token（供 deploy 的 git Basic 认证）；firstNonEmpty 统一「flag > profile config > context preset」取值链；withGateway 把解析出的主机基址补上网关前缀 apiGatewayPath=/api/make（幂等：完整 URL 原样返回），Meta/Repo 客户端构造与 configure verify 共用
- `client_test.go`: 覆盖 resolveAccessToken 取值链（flag > env > 文件 > 空，含 source 标识）、metaServerURL / repoServerURL 四级取值链、resolveContext 优先级（默认 production / settings 覆盖默认 / $MAKE_CLI_CONTEXT 覆盖 settings / --context 覆盖两者 / 未知名报错 / 旧键 environment 拒绝并指引 doctor / flag 绕过旧键守卫）、contextName（成功回显名 / 失败回显 unknown）、resolveChannel（缺省回退 stable / settings 读 beta / 未知值拒绝）与 withGateway 网关前缀拼接（补前缀/幂等/裁尾斜杠/空串），setContextFlag 临时覆盖全局 Context（daemon/trace/errors/context/doctor 测试共用）
- `stdout_test.go`: 测试基础设施，提供 captureStdout 劫持 stdout 的辅助函数 + setProfile(t, name) 临时覆盖全局 Profile（t.Cleanup 还原）
- `context.go`: context 命令组——后端 context（dev/test/production）的用户面，对标 docker context：`list`（别名 ls；tablewriter CURRENT/NAME/META SERVER/AUTH SERVER，当前项标 *，对齐 version list 的 CURRENT 列约定；--output json 平铺全部五个 preset URL + current 布尔）/ `use <name>`（= `settings set context <name>` 的语义糖，经 settings.go setSetting 唯一写路径，回显 Switched to context "x".）/ `show`（回显 resolveContext 解析结果，遵循 flag > env var > settings 链，供脚本判等）；context 是内置 preset 故无 create/rm，自定义地址仍走 profile 的 URL 覆盖键；Long 明说与 app 部署环境（deploy 只推 beta / promote 发 production / delete --env）的区别；list/show 遇旧键配置同样报错指引 doctor
- `context_test.go`: 覆盖 context 命令组的单元测试（list 表格恰有一行标 * 且落在当前 context / JSON 全字段与 preset 一致 / 旧键配置指引 doctor / 非法 output 拒绝、use 持久化并回显 / 未知名拒绝并列合法名、show 遵循解析链 / 缺省 production、子命令与 ls 别名注册），$MAKE_CLI_CONFIG_DIR + setContextFlag + t.Setenv(EnvContext) 隔离全局态
- `doctor.go`: doctor 顶级命令——本地配置体检，**默认只读**（对齐 brew/flutter/npm doctor 先例与 linter 的 `--fix` 惯例），`--fix` 才落盘，是旧配置升级到新格式的唯一通道（解析链遇旧键只报错指引 doctor --fix，不背兼容包袱；doctorFixHint 常量两处同文）：doctorChecks() 按序组装检查表（settings 旧键 → fix 调 config.MigrateSettings 原地搬家；随后遍历 settings.go 的 settingKeys 表经 checkSetting 逐键校验——未设置即缺省不算问题、非法值附 `makecli settings set <key> <value>` 指引，新增全局键零改动；最后 token：当前 profile 拿得到 token 含来源），每项重新读盘故前项的 fix 对后项立即可见（搬家后 context 检查读到的已是新键并照常校验旧值）；渲染对齐 preflight（✓ 通过、✗ 问题；名字列按最长检查名对齐；可修项只读模式标 `(fixable, run: makecli doctor --fix)` 并计入问题，--fix 模式当场修复打 ✓ fixed 不计数；修不了附 next-step 命令），末尾 OK / FAIL: N problems left；存在未修复问题返回 errDoctorFailed（退出码 1，errors.go 静默哨兵）
- `doctor_test.go`: 覆盖 doctor 的单元测试（默认只读：旧键标 fixable 指引 --fix、文件不动、退出 1 / --fix 把 environment 搬到 context 且其后 resolveContext 可用、不再打 fixable / 健康配置两种模式均 OK 无 ✗ / 未知 context 与 channel 各报一行附 settings set 指引、不标 fixable 并 FAIL 计数 / 缺 token 指引 login --profile / credentials 来源识别 / --fix 后旧值非法时同轮 context 检查读到新键报问题 / 哨兵静默且退出码 1 / --fix flag 注册且默认 false），healthyDoctorEnv 组装隔离基线，squash + hasLine 让行断言不依赖名字列宽
- `app.go`: app 命令组，挂载 create / init / list / info / delete / deploy / clone / pull（均 Hidden）/ promote 子命令；提供 loadAppManifestFromFile 共享 helper（从 YAML 加载唯一 Make.App 资源）
- `git.go`: cmd 模块共享 go-git 原语层（与具体命令无关），被 app_init / app_create / deploy / clone 复用：openRepo（只开不 init，缺失即指引 app init，DetectDotGit 向上探测）、initGitRepo（PlainOpen 不探测父仓库→该目录自身建 .git，幂等）、ensureGitignore（增量补齐，agents/gitignore.tmpl 做期望条目单一真相源，缺则追加 `# added by makecli`，文件不存在写模板全文）、gitSignature（user.name/email 缺省回退 makecli；用 LocalScope 合并 system+global+local，修正原 SystemScope 读不到全局 gitconfig 的 bug）、stageAndCommit（add -A 尊重 .gitignore，干净则不造空提交）、assertDeployable（有 HEAD + assertClean）、assertClean（工作树干净门控，HEAD 可无——空仓库也算干净；hint 参数只换指引文案，deploy 要求先 commit、clone 要求 commit 或清理，门控同一条）；命令专属 git 约定（部署分支/匿名 remote/push/fetch+ff）仍内聚 deploy.go / clone.go
- `git_test.go`: 覆盖 git.go 共享原语的单元测试（建仓幂等 / .gitignore 写全+增量补齐+完整不重写 / stageAndCommit 提交+空树no-op+尊重gitignore / assertDeployable 无HEAD拒绝+干净通过+脏树拒绝 / openRepo 缺失指引 app init / gitSignature 回退 makecli + LocalScope 读本地配置回归守护），t.TempDir 隔离 FS + 真实 go-git
- `app_create.go`: app create 子命令——= app init 本地脚手架内核 + 远端 App 注册 + initial commit。代码仓库不在 create 阶段准备：服务端要求先在 Make Console 建 Beta 环境（否则 409 MAKE_BETA_ENVIRONMENT_REQUIRED），仓库由 deploy 经同一幂等接口按需准备。位置参数 <appKey> 同时是「目录名 + key」（deriveAppKey 取 filepath.Base(filepath.Abs(arg)) 推导，覆盖 shop/`.`/`..`，validResourceKey 把关）；--name 展示名（缺省=key），--description；执行序「远端先行」 解析key→加载凭证→CreateApp(远端,先于任何本地写)→writeScaffold(幂等 skip-if-exists 写 CLAUDE.md/AGENTS.md/apps/dsl/app.yaml,返回新建清单;scaffoldFile{embed,out} 映射模板名;scaffoldOutputs 收口顺序;fileExists 只读判定)→scaffoldGit(initGitRepo+ensureGitignore+stageAndCommit「Initial scaffold for <key>」→create 产物即干净可立即 deploy;git 失败降级 stderr 警告不阻断已成功的远端创建,全程不写 stdout 保持成功输出一行)——新目录远端失败(坏token/冲突/网络)时本地零残留,换 profile 重跑干净；writeScaffold 幂等故去掉旧 assertScaffoldClear 硬拒，create 可与 init 组合(先 init 本地再 create 补远端,不覆盖用户编辑)；成功只打印 `App 'X' created successfully` 一行；newAppManifest 单一构造点（脚手架与 CreateApp 共用,被 app_init 复用,空 description 不进 properties 避免 diff 漂移）；renderAppDSL 用 ResourceManifest+yaml.Marshal(2空格缩进) 写出可被 apply/diff 读回的 app.yaml；-f 文件模式仅建远端不脚手架；--dry-run 经 api.WithDryRun 注入 X-Dry-Run 让远端校验不落库，校验通过即收尾（跳过脚手架/git 等真实副作用），脚手架与文件两模式同款短路
- `app_create_test.go`: 覆盖 renderAppDSL / writeScaffold / runAppCreate / runAppCreateFromFile 的单元测试（DSL渲染+往返/脚手架文件不含.gitignore且报新建清单/AGENTS.md 关键合同经 assertGeneratedAgentsContract 校验/幂等skip-if-exists保留用户编辑/scaffolds 干净 git 仓库带 initial commit 即可 deploy/appKey推导含'.'/成功输出简洁无仓库信息/仓库失败 stderr 警告但 create 成功/API失败本地零残留/与预先 init 脚手架组合不硬拒且保留编辑/无凭证不留半成品/未知profile/文件模式/非法目录名拒绝），用 httptest 隔离网络 + t.TempDir 隔离 FS + chdir helper 测 '.' 推导 + captureStderr 测降级警告；stubRepoServer 临时指向仓库 mock；含 validResourceKey 通用 key 校验测试（长度 2-20，不可以下划线开头）
- `app_init.go`: app init 子命令——把目录变成完整本地 Make app 项目（与 create 共享同一脚手架内核，create = init 核心 + 远端注册 + initial commit）：runAppInit 复用 deriveAppKey/newAppManifest/writeScaffold（app_create）+ initGitRepo/ensureGitignore（git.go）写 CLAUDE.md/AGENTS.md/apps/dsl/app.yaml + git init + .gitignore，逐项 statusWord 打印 created/exists·initialized/already·updated/complete 状态行；可选 [appKey]（兼目录名，deriveAppKey 推导，缺省取 cwd 名，validResourceKey 把关），全程幂等 skip-if-exists，刻意不 commit（提交时机交还用户）、不碰远端；deploy 的 openRepo 门控依赖此命令先行建仓
- `app_init_test.go`: 覆盖 runAppInit 的单元测试（完整脚手架 CLAUDE.md/AGENTS.md/app.yaml/.gitignore/.git + AGENTS.md 关键合同经 assertGeneratedAgentsContract 校验 / app.yaml key 取目录名 / 缺省 appKey 取 cwd 名 / 非法目录名拒绝 / 幂等再跑报 exists·already·complete 且保留用户编辑 / 刻意不 commit→refs/heads 为空无分支），t.TempDir 隔离 FS + chdir 测默认 cwd + captureStdout 验证状态行
- `agents_contract_test.go`: app init / app create 共享的 AGENTS.md 合同断言，集中校验 App Contract、统一登录、Service 代理、运行时产物、发布前验证和旧标记禁用，避免两个测试类重复锁定大量自然语言文案
- `app_list.go`: app list 子命令，调用 MakeService.ListResources 分页列出 org 下全部 App，输出列 KEY/NAME/DESCRIPTION/VERSION/CREATED AT（description 取自 app.Properties["description"]，空则留空格）；支持 --profile / --server / --page / --size / --filter flags；parseFilter 把 "key=value" 过滤语法翻译为 CEL 表达式文本（key 走等值 `key == 'v'`，name/description 走 `field.contains('v')`，逗号 = `||`），celString 转义单引号字面量；服务端按 Expression{expression} 解析（见 AgenticDSL/Design/ExpressionDesign.md）
- `app_list_test.go`: 覆盖 runAppList / parseFilter 的单元测试（成功/空列表/分页JSON/过滤请求/非法过滤/无凭证/API错误/非法页码），用 httptest 隔离网络
- `app_info.go`: app info 子命令，展示单个 App 的元信息 + 双环境部署状态：顺序编排 GetApp（ErrNotFound → 「app 不存在」报错）与 GetDeploymentOverview（ErrNotFound → 从未部署合法状态，占位继续），头部 %-13s 平铺 Key/Name/Description/Version/Created At + tablewriter 一张表 ENVIRONMENT/STATUS/COMMIT/URL（固定 beta/production 两行，deploymentRow 对 nil 环境渲染 Not deployed 占位行，URL 不截断）；--output table|json（JSON 输出 {app, deployment} 完整字段，从未部署时 deployment 为 null）
- `app_info_test.go`: 覆盖 runAppInfo 的单元测试（双环境渲染/单环境占位/404 从未部署占位且退出 0/app 不存在报错/部署服务 500 失败/JSON 完整字段/JSON null deployment/非法格式/非法 key 触网前拦截/无凭证/未知 profile/info 挂载断言），newAppInfoServer 按带 /api/make 网关前缀的路径分流 mock 两个接口 + httptest 隔离网络
- `app_delete.go`: app delete 子命令，调用 Meta Server API（MakeService.DeleteResource，POST /meta/v1/app?appRole=prod|beta）删除 App 对的一半：--env production|beta|all 必填、大小写不敏感（app 命令组内唯一的 --env，与全局 --context 正交），resolveDeleteEnvs 纯函数把 --env 展开成有序环境序列——production / beta 各一步，all 先 beta 后 production（服务端要求先清 beta）；每一步用 newClientFromProfile(api.WithAppRole(api.RoleForEnv(env))) 发「用户给的 prod key + ?appRole=」交服务端定位实际目标，CLI 不反查 pairAppKey、不触 GetApp（删除路径零读请求，测试 mock 见到任何非 DeleteResource target 即判失败）；prod 有 beta 配对时服务端 409 拒删须先删 beta，其 msg 原样上抛不套环境前缀（api 层非唯一性 409 直接回服务端 msg，给用户的就是那句话）。用户面只见「app key + 环境」：确认表单敲用户给的 app key（标题注明环境）、成功提示逐环境一行形如 App 'x' beta environment deleted successfully；删除前要求原样输入 app key 确认（gh repo delete 同款强护栏，charm.land/huh/v2 表单 + Validate 阻断错值提交、ErrUserAborted 视为取消；非交互终端经 go-isatty 检测直接拒绝并指引 --yes，杜绝挂起），--yes/-y 跳过确认；confirmDeleteFunc 包级可打桩变量隔离终端交互；支持 -f YAML 文件模式
- `app_delete_test.go`: 覆盖 resolveDeleteSteps（production 不反查、beta 取服务端 pairAppKey、传入 beta app 拒绝、无配对拒绝、all 有序两步/无配对一步、空或未知 env）与 --env ALL 端到端（顺序 + 确认一次），newMockPairMeta 按 X-Make-Target 分流 Get/Delete 并按序记录删除 key与 runAppDelete / runAppDeleteFromFile 的单元测试（--env beta 时确认收用户 key、API 收服务端 pairAppKey / --yes 删除 / 确认通过后删除 / 确认拒绝在触网前短路 / 非交互真门控拒绝 / 无凭证 / API错误 / 未知profile / 文件模式），stubConfirm 打桩 confirmDeleteFunc 隔离终端交互 + httptest 隔离网络
- `entity.go`: entity 命令组，挂载 create / delete / list 子命令；--app（appKey）参数为子命令继承
- `entity_create.go`: entity create 子命令，位置参数为 Entity key，--name 为展示名（缺省回退 key），--json 加载整个 properties（fields + uniqueConstraints，同形于 DSL 的 properties 与 entity list -o json 的 data.properties，三者可往返）；校验 field.Key 合法（validResourceKey）+ 约束字段引用（validateConstraintFieldRefs：name 的引用字段须在已声明 fields 内、≥1 字段；类型白名单/配额/存量重复交服务端裁决）；--app 传 appKey；--dry-run 经 api.WithDryRun 注入 X-Dry-Run 远端校验不落库，成功打印 would-be 行；loadEntityProperties 从 JSON 文件加载 api.EntityProperties 透传
- `entity_create_test.go`: 覆盖 runEntityCreate / validateConstraintFieldRefs 的单元测试（成功/带fields/field key 校验/无凭证/API错误/未知profile/非法JSON + --json properties 的 uniqueConstraints 到达线缆/未声明字段拒绝/字段引用校验），用 httptest 隔离网络
- `entity_delete.go`: entity delete 子命令，按 key 调用 Meta Server API（MakeService.DeleteResource）删除指定 Entity；--app 传 appKey
- `entity_delete_test.go`: 覆盖 runEntityDelete 的单元测试（成功/无凭证/API错误/未知profile），用 httptest 隔离网络
- `entity_list.go`: entity list 子命令，按 appKey 分页列出 entity（KEY/NAME/VERSION），位置参数为 entity key 时显示详情（Key/Name/App/Version + fields 表格 KEY/NAME/TYPE + 唯一性约束表 NAME/FIELDS，renderUniqueConstraints 无约束则静默）；--app 传 appKey；复用 parseFilter
- `entity_list_test.go`: 覆盖 runEntityList 的单元测试（列表/空列表/过滤请求/具体entity/无字段/唯一性约束表/无凭证/API错误/未知profile），用 httptest 隔离网络
- `relation.go`: relation 命令组，挂载 create / update / delete / list 子命令；--app（appKey）参数为子命令继承
- `relation_create.go`: relation create 子命令，位置参数为 Relation key，--name 为展示名，--json 加载 from/to（entityKey 引用），调用 Meta Server API 创建 Relation；--dry-run 经 api.WithDryRun 注入 X-Dry-Run 远端校验不落库，成功打印 would-be 行
- `relation_create_test.go`: 覆盖 runRelationCreate 的单元测试（成功/无凭证/API错误/未知profile/非法JSON/文件不存在），用 httptest 隔离网络
- `relation_update.go`: relation update 子命令，按 key 定位，--name 更新展示名，--json 加载 from/to（entityKey 引用），调用 Meta Server API 更新 Relation
- `relation_update_test.go`: 覆盖 runRelationUpdate 的单元测试（成功/无凭证/API错误/未知profile/非法JSON），用 httptest 隔离网络
- `relation_delete.go`: relation delete 子命令，按 key 调用 Meta Server API 删除指定 Relation
- `relation_delete_test.go`: 覆盖 runRelationDelete 的单元测试（成功/无凭证/API错误/未知profile），用 httptest 隔离网络
- `relation_list.go`: relation list 子命令，按 appKey 分页列出 relation（KEY/NAME/FROM/TO/VERSION，FROM/TO 显示 entityKey(cardinality)），位置参数为 relation key 时显示详情；复用 parseFilter
- `relation_list_test.go`: 覆盖 runRelationList 的单元测试（列表/空列表/JSON列表/详情/JSON详情/过滤请求/无凭证/API错误/未知profile/非法页码/非法格式），用 httptest 隔离网络
- `record.go`: record 命令组，挂载 create / get / update / delete / list / aggregate 子命令，Example 每个子命令一条示例（对齐 configure 组的父命令示例约定）；--app（appKey）和 --entity（entityKey）参数为子命令继承
- `record_create.go`: record create 子命令，从 JSON 文件加载动态 KV 数据，调用 Data Service API 创建 Record，输出 recordID；loadRecordData 从 JSON 文件加载记录数据；支持 --app（继承）/ --entity（继承）/ --json（必选）/ --dry-run（经 api.WithDryRun 注入 X-Dry-Run 远端校验不落库，成功打印 would-be 行、不展示回滚事务 recordID）/ --profile
- `record_create_test.go`: 覆盖 runRecordCreate 的单元测试（成功/无凭证/API错误/未知profile/非法JSON/文件不存在），用 httptest 隔离网络
- `record_get.go`: record get 子命令，获取单条 Record 并按 key 排序逐行输出或 JSON 格式展示；支持 --app（继承）/ --entity（继承）/ --profile / --output
- `record_get_test.go`: 覆盖 runRecordGet 的单元测试（成功/JSON输出/无凭证/API错误/未知profile/非法格式），用 httptest 隔离网络
- `record_update.go`: record update 子命令，透明路由——1 个 recordID 走 /data/v1/record 单条更新，N 个走 /data/v1/field 批量更新；支持 --app（继承）/ --entity（继承）/ --json（必选）/ --profile
- `record_update_test.go`: 覆盖 runRecordUpdate 的单元测试（单条路由验证/批量路由验证/无凭证/API错误/未知profile/非法JSON），用 httptest 隔离网络，重点验证请求路径
- `record_delete.go`: record delete 子命令，批量删除 Record，汇报每条记录的删除结果；支持 --app（继承）/ --entity（继承）/ --profile
- `record_delete_test.go`: 覆盖 runRecordDelete 的单元测试（单条/批量/部分失败/无凭证/API错误/未知profile），用 httptest 隔离网络
- `record_list.go`: record list 子命令，分页查询 Record，自动从首条记录提取列名或使用 --fields 指定列，extractKeys 提取 map 键；--sort-json 是 JSON 型 flag（元素 {fieldKey,order}，经 decodeJSONFlag 直透）；--filter 直收 raw CEL 表达式（不在 CLI 解析，原样塞进 filter.expression，服务端裁决合法性；命令带 Long+EXAMPLES 教 CEL 用法）；包内 checkPagination（page/size 下界）/ upperHeaders（大写表头）供 record aggregate 复用；支持 --app（继承）/ --entity（继承）/ --profile / --page / --size / --output / --fields / --sort-json / --filter
- `record_list_test.go`: 覆盖 runRecordList 的单元测试（表格/JSON/空列表/无凭证/API错误/未知profile/非法页码/非法格式/非法 sort-json/sort-json 直透为 sort 数组/filter 直透），用 httptest 隔离网络
- `record_aggregate.go`: record aggregate 子命令，对单个 Entity 做服务端聚合统计（api.AggregateRecords → /data/v1/aggregate，声明式 GROUP BY，契约见 AgenticDSL Design/DataAPIDesign.md「聚合统计 Record 数据」）。flag 与 API 字段机械映射——JSON 型 = 字段名 + `-json`（--group-json 分组维度、--aggregates-json 聚合指标（必填）、--sort-json 排序，元素形状写在 Long 里），CEL 文本 = 字段名 kebab-case（--filter 聚合前 WHERE、--aggregate-filter 聚合后 HAVING 仅引用指标 alias）；CLI 只做结构解码，granularity / aggregate 取值与 alias 必填均由服务端裁决（Long 里说明 group fieldKey 须为 schema 输出 capabilities.aggregable=true 的字段，与列表分组 groupable 独立，CLI 不预校验）；表格列序取自请求声明（aggregateColumns：group 取 alias 缺省 fieldKey，再接 aggregates 的 alias），不从响应首行猜；formatAggregateCell 维度列 {value,label} 显示 label、指标 float64 用 FormatFloat 'f' 避免 %v 科学计数法；不分组即单行全局聚合，不是特殊情况；支持 --app（继承）/ --entity（继承）/ --profile / --page / --size（默认 10，对齐服务端缺省）/ --output
- `record_aggregate_test.go`: 覆盖 runRecordAggregate 的单元测试（请求体直透含 sort alias 不带 fieldKey / 表格列序与 label、数值渲染 / 可选字段省略 / JSON 输出 / 空结果 / 三个 JSON flag 非法值均报错带 flag 名 / 非法分页与格式 / 无凭证 / API 错误透出）+ formatAggregateCell 表测，用 httptest 隔离网络
- `deploy.go`: app deploy 子命令（`makecli app deploy`）——「纯 push 已提交状态」（提交时机交还用户，不再自动 add/commit）。目标只有 beta（无 --env、无确认：production 没有本地 push 入口，只能经 app promote 从 beta 发布，「先 beta 后 production」是结构约束；用户面 beta 对应服务端 preview key，翻译收口 api.ServerEnvKey/DisplayEnv）；app key 取自 apps/dsl/app.yaml（appKeyFromDSL：app 身份单一真相源，文件缺失即给可操作错误，无 --app 旋钮；appKeyFromManifest(path) 是其按路径读取的内核，clone 复用）；执行序本地门控先行(fail-fast,网络之前)：openRepo(git.go,缺 .git 即报错指引 app init)→assertDeployable(git.go,无 commit/工作树脏即报错让用户先 commit)→betaRepoURL（与 clone 共用的 beta 仓库定位：assertAppRegistered(newClientFromProfile+Meta GetApp 把关 app 已注册：ErrNotFound 即指引 `makecli app create -f apps/dsl/app.yaml` 避免「有仓库、无 app」孤儿状态，其余错误原样上抛不放行；在建仓库之前短路)→app.KeyForEnv(beta) 定位承载 beta 的 app→newRepoClientFromProfile→CreateRepository(MakeService.CreateResource 幂等)取 cloneUrl+token，缺 cloneUrl 报错）→打印 App/Environment→pushCurrentHead；pushHead 用 go-git/v5(纯 Go,二进制自包含)：CreateRemoteAnonymous 承载 cloneUrl 不落 .git/config，推 head→refs/heads/<deployBranch> 常量 dev webhook 约定，HTTP BasicAuth make:<token>，NoErrAlreadyUpToDate 视为成功；--force 透传 PushOptions.Force；gitPushFunc 包级变量便于测试打桩（仅隔离推送，本地仓库门控跑真 go-git）；建仓+.gitignore+commit 已下沉 app init / app create（git.go）；--status 短路部署改走只读进度查询（runDeployStatus：app.yaml+openRepo 与 deploy 同源门控→HEAD sha→api.GetBuildTask 按 commitSha + environment 反查构建任务（deploy 只推 beta，environment 固定传 api.EnvBeta）→renderBuildStatus 以 %-12s 平铺 App/Environment/Build#id/Version/Commit/Status/Phase/Error/Image/Created/Started/Finished，可选字段无值不渲染行、Error 行只在失败任务自然出现无 status 分支；ErrNotFound 翻译成「尚无构建任务→先 app deploy 或稍后重试」引导；formatBuildError 拼错误码+信息；shortSha 短 sha helper 与 pushCurrentHead 共用）；--wait 阻塞至构建终态（deploy --wait = push 后接上与 --status --wait 完全同一条等待路径，消除双路径特例）：waitForBuild 以 buildPollInterval=3s（包级可打桩）轮询 GetBuildTask，ErrNotFound 视为「任务尚未创建」继续等（webhook 异步建任务窗口期，由 timeout 统一兜底），进度只在 status/phase 跃迁时打一行；waitAndRenderBuild 终态渲染完整详情后，未成功以 errBuildFailed、超时以 errWaitTimeout 上抛（退出码哨兵，errors.go ExitCode 译为 2/124，终态判定走 api 层 Finished/Succeeded 不在 cmd 判等状态串）；--timeout 有界等待（默认 defaultWaitTimeout=5m，须与 --wait 搭配否则拒绝）；--output table|json 经 renderBuildResult 分流（json 仅限 --status 模式——deploy 推送输出会混入 stdout；wait+json 进度走 stderr、stdout 只留最终状态对象），旗标组合校验在 RunE 入口先于一切门控；成功任务经 deploymentURLFor→envURLFor（与 promote 共用的环境 URL 取址）带出对应环境访问 URL（与 app info 同源 GetDeploymentOverview + Env 选择器按 task.Environment 取址；URL 是结果装饰：仅 SUCCESS 查询、失败不展示（线上仍是旧 release）、总览失败降级为空不影响主输出；table 尾行 URL:、json 经 buildStatusView 平铺 url omitempty 字段）
- `deploy_test.go`: 覆盖 runDeploy 编排（pushCall 打桩 gitPushFunc 隔离推送，gitCommitAll 建真 committed 仓库过门控：推到 beta 配对 app 仓库与 --force 透传、app.yaml 取 key、app.yaml 缺失/key 非法、fail-fast 无.git/无commit/脏树报错且 noNetRepoServer 证不触网、app 未注册指引 app create 且短路不触仓库、TestDeployTargetsHostingApp 锁定仓库建在承载 beta 的 app 名下（prod 的 beta → pairAppKey、beta app → 自己、无配对报错不触仓库）、注册校验错误不误判为「不存在」、无凭证、API 错误、cloneUrl 缺失、push 错误透传）+ pushCurrentHead 真 go-git（newBareRemote 本地裸仓库做 file-transport push 目标：推已提交 HEAD 到 main 分支、干净 redeploy 为 up-to-date no-op、新提交后推更新 main）+ runDeployStatus（newBuildTaskMeta 记录请求的 Meta mock + headSha 取真仓库 HEAD：按 HEAD sha 发 /api/make/build/v1/build GetResource 并渲染、软空响应给「尚无构建任务→deploy」引导、app.yaml 缺失/无仓库/无提交 fail-fast、无凭证报错；TestDeployStatusFlagShortCircuitsDeploy 走真 cobra 证 --status 只读不 push 不触仓库服务；TestRenderBuildStatus 直测失败任务 Error 行与空可选行不渲染 + formatBuildError 单侧字段）+ --wait 等待（TestRunDeployStatusWait：轮询至终态并渲染详情、跃迁行去重、not-found 窗口期容忍、FAILED/CANCELED 均 errBuildFailed 且带错误详情、超时 errWaitTimeout 并引导 --status、查询错误即刻失败不误判、json 模式 stdout 纯 JSON 进度走 stderr；TestDeployWaitAfterPush 走真 cobra 证 --wait 先推送后阻塞；TestDeployFlagValidation 走真 cobra 证旗标组合校验先于门控：json 需 --status、--timeout 需 --wait、非正 timeout 拒绝、非法格式拒绝；TestDeployStatusJSONSnapshot 快照 json 可解析；成功 URL：wait 表格尾行/production 选址不串环境/失败无 URL 行/总览 500 降级不阻断/json url 字段/TestDeployStatusSnapshotURL 快照同路径；newBuildSeqMeta 按序答复快照模拟状态推进兼答注册门控与部署总览 URL 夹具 + stubPollInterval 调小轮询间隔 + taskSnap 快照构造），enterAppDir 临时工程目录隔离 FS + httptest 隔离网络（newAppExistsMeta 放行注册门控 / stubMetaServer 临时指向 Meta）
- `clone.go`: 隐藏命令 `makecli app clone <appKey>`；appKey 必填，resolveCloneTarget 克隆到 ./<appKey> 并校验已有清单身份；assertCloneTarget 在网络前检查目录和工作树，betaRepoURL 成功后 prepareCloneRepo 才建目录；runAppSync 供 clone/pull 共用编排，cloneDeployBranch 经匿名 remote fetch 到 make/main 并 ff-only 更新，拒绝分叉、不落远端凭证；gitCloneFunc 供测试打桩。
- `pull.go`: 隐藏无参命令 `makecli app pull`；从当前工程 apps/dsl/app.yaml 读取 appKey，要求当前目录已有 Git 仓库，再复用 runAppSync 更新当前目录。
- `pull_test.go`: 覆盖隐藏命令注册、位置参数拒绝、清单身份、更新当前目录，以及缺少清单/仓库、非法 key、脏树拒绝。
- `clone_test.go`: 验证 clone 命令注册与隐藏状态、缺参和多参拒绝、参数和身份解析、网络前目录门控、失败零残留，以及本地裸仓库上的首次检出、重复克隆、快进、本地领先和分叉保护。
- `promote.go`: app promote 子命令（`makecli app promote`）——对标 vercel promote：把 beta 环境当前生效的版本发布到 production。输入是服务端的 beta 状态而非本地代码（不 push、不读本地 git），「先 beta 后 production」因此是结构约束而非校验分支。执行序：appKeyFromDSL→assertAppRegistered（与 deploy 同源）→resolvePromoteTarget 经 KeyForEnv 定位 beta/prod 两个 app（无 beta 配对给可操作错误）→promoteSummary 用 GetDeploymentOverview(prodKey) 取 beta/production 当前 commit 短 sha 打 App/Source/Target 摘要（beta 从未部署 fail-fast 在确认之前；总览查询失败降级 unknown 交服务端裁决）→production 确认（confirmProductionPromote：charm.land/huh confirm + go-isatty，confirmPromoteFunc 包级可打桩，--yes/-y 跳过，非交互终端拒绝并指引 --yes）→api.PromoteApp(betaKey)（服务端 Temporal 异步流程，回执 promoteId）→打印 `Promote ID:  <promoteId>`（%-12s 与摘要同款）+ `promote --status --id <promoteId>` 指引（json 模式输出 promoteReceipt{app,betaApp,promoteId}）。promoteId 是发布的唯一句柄，由用户显式带回：--status 必须搭配 --id（RunE 入口校验 status == (id != "")，缺半边即拒绝），CLI 不落盘、不猜「最近一次」；--status 查 api.GetPromoteStatus(betaKey, promoteId) 渲染；--wait 阻塞至终态（promote --wait = 发起后接上与 --status --wait 同一条等待路径 waitAndRenderPromote/waitForPromote：buildPollInterval 轮询，ErrNotFound 视为「工作流尚未可查」继续等，进度只在 state/step 跃迁时打一行，json 模式进度走 stderr），未成功 errPromoteFailed（退出码 2）、超时 errWaitTimeout（124），--timeout 默认 defaultPromoteTimeout=10m（发布串联配置同步+构建+部署，比单次构建长）须与 --wait 搭配；renderPromoteStatus 以 %-12s 平铺 App/Beta app/Run/Type/State/Step/Message/Beta build/Prod build + Steps 缩进列表（state 定宽）+ 成功后 URL（productionURLFor 经 envURLFor 取 production 地址，仅 SUCCEEDED 查询）；json 经 promoteStatusView 平铺 app/betaApp + PromoteStatus + url omitempty
- `promote_test.go`: 覆盖 runPromote（promoteMeta 按路径 + X-Make-Target 路由的 Meta mock：CreateResource 以 beta key 发起并打印 Promote ID + --status --id 指引、abort 短路不触达发布接口、--yes 跳过确认、beta 从未部署 fail-fast、总览 500 降级 unknown 仍发布、无 beta 配对报错、--wait json stdout 纯 JSON 含 app/betaApp/url）+ runPromoteStatus（--status/--id 缺半边经 newPromoteCmd().Execute 在 RunE 入口拒绝、请求携带 beta key + 给定 promoteId 且运行中不渲染 URL、--wait 轮询至 SUCCEEDED 跃迁去重并渲染 URL、FAILED → errPromoteFailed 带 Message 无 URL 且 ExitCode=2、not-found 窗口期容忍后 errWaitTimeout）；stubConfirmPromote 打桩确认并计数，setupPromote 复用 enterAppDir/saveDefaultToken/stubPollInterval/stubMetaServer 并隔离 HOME
- `preflight.go`: preflight 顶级子命令，以 make-build-service build_spec.md 第 5 节检查清单为实现依据（可选位置参数 [dir]，默认 cwd；--app-type 已移除）：构建模式 A/B 自动判定（apps/ui|service/package.json 任一存在→A，否则 B）、包管理器按 lockfile 优先级判定（pnpm>yarn>npm，检测目录 A=apps/ B=根；无 lockfile 回退 npm）；buildPreflightContext 一次收集事实（各 package.json/pnpm-workspace.yaml 三态投影 pkgFile、app.yaml 单次解析得出 appKey/appYAMLReadable 事实，resolveRepoName 复用该事实取 key 回退目录名而非重新解析），preflightChecks 表驱动求值（条目与 spec 1:1：G1/G2/P1、A1-A9/A11/A15(TEMP)、B1-B3 + makecli 自有 D1=apps/dsl/ 存在且 app.yaml key 可读（目录在但 key 读不出同样报错，堵住「preflight 绿、deploy 红」缺口）；BUILD-TIME 启发式 A10/A12/A13/A14/B4 本版不做），A8 刻意不限模式（spec §7 要求孤儿组件目录回退 B 时同报 A8+B1）；ERROR/WARN/INFO 三级输出（✓/✗/!/i + 检查 ID），失败条目附 How to fix 块（每条 fix 指引：为什么+改哪+改成什么，面向 AI agent 一步收敛；A15 按 ctx.lockfiles 是否为空分支，无 lockfile 可 import 时不建议 `pnpm import`）；存在 ERROR 返回 errPreflightFailed（退出码 1），仅 WARN/INFO 放行，作 CI/deploy 前置门禁
- `preflight_test.go`: 覆盖 preflight 的单元测试（文件投影 workspacesField 双形态/pkgFile 三态/pnpm-workspace 解析、lockfile 优先级与 npm 回退、workspaceCovers glob 各形态、上下文构建含孤儿组件目录不触发模式 A、repoName 两来源、检查表全条目含 spec §7 常见失败结构 8 行与 A8+B1 同报、渲染与退出码含 How to fix 块/仅警告放行/--app-type 已移除），evalChecks 白盒跑表收集分级 id + pfModeAPnpm/pfModeAYarn/pfModeB 全绿夹具做减法 + t.TempDir 隔离文件系统；TestPreflightChecksTableInvariants 守护表不变式（ERROR/WARN 必有 fix、ERROR 必有 label）；D1 补测 app.yaml 缺失/不可读 YAML/key 为空三种「目录在但 key 读不出」场景；TestA15FixHint 锁定 fix 按 ctx.lockfiles 分支（有 lockfile 建议 pnpm import，无则只建议 pnpm install）
- `apply.go`: apply 子命令，从 YAML 文件/目录批量应用资源（create-or-update 语义：按 Key 检测存在性，App 不存在则创建/已存在则跳过，Entity/Relation 不存在则创建/已存在则更新）；存在性判定经 api.ErrNotFound 哨兵——仅"确实不存在"才创建，Get 的瞬时/传输/非 not-found 错误一律上抛不创建（杜绝误建重复资源或把 update 降级为 create）；依赖顺序 App→Entity→Relation；ResourceManifest 提供 Key（标识符）/Name（展示名）/Type/AppKey/Meta/Properties 字段；applyEntity 经 extractUniqueConstraints 从 properties.uniqueConstraints 结构性透传唯一性约束（合法性交服务端）→构建 api.EntityProperties 写入；支持多文档 YAML；目录扫描经 loadManifestsFromDir（filepath.WalkDir）递归，--max-depth 控深度（1=当前目录/2=含子目录默认/0=不限，负值拒绝），隐藏文件与隐藏子目录（.git 等）跳过，subdirLevel 算相对根层级做剪枝；loadManifestsFromDir/subdirLevel 被 diff 复用
- `apply_test.go`: apply 子命令的单元测试，覆盖单文件、多文档、目录扫描、Relation 创建/更新/缺 appKey 字段错误、App+Entity+Relation 混合目录场景、uniqueConstraints 到达线缆 + extractUniqueConstraints 解析/错误
- `diff.go`: diff 子命令，对比远端 Meta Server 上 App **beta 环境**的 DSL（Entity + Relation + 唯一性约束）与本地 YAML 文件的差异——本地代码经 deploy 只推 beta、production 由 promote 发布，故基线固定 beta：client 经 api.WithAppRole(api.RoleBeta) 让每个远端请求带 ?appRole=beta，无 --env 旋钮；App key 从 YAML 自动推断（Make.App key 或 Entity/Relation appKey 字段）；分页获取全部远端资源，按 Key 匹配后逐字段（key）/端点（entityKey）/唯一性约束（compareConstraints 按 name 匹配、fields 顺序敏感 slices.Equal）比对，约束差异并入 EntityDiff.Constraints 且影响 entity changed 判定；支持 -f（必选）/ --output / --max-depth（与 apply 同款，默认 2，0=不限，复用 loadManifestsFromDir，保证 diff/apply 目录展开一致）；退出码 0=无差异 1=有差异（table 与 json 模式一致，经 errDiffFound 哨兵上抛到 main 转译，不再用 os.Exit，可单测）；真实错误与 errDiffFound 哨兵均交由 reportExecuteError（errors.go 单一出口）处理——哨兵静默、真实错误打印
- `diff_test.go`: 覆盖 diff 子命令核心逻辑的单元测试（computeDiff/computeRelationDiff/computeDiffUniqueConstraints/fetchAllEntities/fetchAllRelations/jsonDeepEqual/runDiff 错误路径），用 httptest 隔离网络
- `schema.go`: schema 顶级子命令，按 appKey 调用 MakeService.GetResource 获取聚合 Schema（App + Entities + Relations），JSON 输出
- `schema_test.go`: 覆盖 runSchema 的单元测试（成功/无凭证/API错误/未知profile），用 httptest 隔离网络
- `jsonflag.go`: JSON 型 flag 的单一入口（对齐 lark-cli）：readJSONFlag 按取值三形态取字节——`-` 读 stdin（flagStdin 包级可打桩，每次调用只能一个 flag 用）、`@path` 读文件、其余视为内联 JSON；decodeJSONFlag 用 DisallowUnknownFields 严格解码，key 拼错本地即报且错误带 --name 定位，取值合法性交服务端裁决；jsonFlagForms 是帮助文案公共后缀。命名约定：JSON 型 flag = API 字段名 + `-json`（record list --sort-json、record aggregate --group-json/--aggregates-json/--sort-json）
- `jsonflag_test.go`: 覆盖 decodeJSONFlag 三形态（inline/@file/stdin 打桩）与严格解码（未知字段拒绝且错误含 flag 名与 key / 非 JSON 值 / 文件不存在）的单元测试
- `output.go`: `--output` 三态 auto|table|json 的单一真相源——addOutputFlag 统一注册旗标（默认 auto），resolveOutputFormat 解析：auto 按 stdout 是否 TTY 落到 table（人在终端）或 json（管道 / CI / agent 调用），显式值透传，非法值拒绝；stdoutIsTerminal 包级可打桩探针；deploy 无 --status 时 auto 先落回 table（json 在该模式非法，不能把 agent 挡在报错上）；所有 `--output json` 的唯一 stdout 出口（writeJSON 编码），被 app list / entity list / relation list / record list / record aggregate / record get / whoami / app info / deploy --status / diff / schema / configure verify 等复用；writeJSON 读 notifier.Pending()，有待提示更新时经 attachNotice 在顶层对象末尾追加 `_notice.update`（字节级追加成员，不解析不重排，原字段顺序不动；顶层为数组/null 无处可挂则原样），对齐 lark-cli 让 agent 在非 TTY 下也收到升级提示
- `output_test.go`: 覆盖 resolveOutputFormat 的 auto 按 TTY 落定 / 显式值透传 / 非法值拒绝、addOutputFlag 默认 auto（stubStdoutTerminal 打桩探针供全包测试复用）；writeJSON 无提示时输出逐字节不变 / 有提示时 _notice.update 追加为末尾成员且原字段顺序不动 / attachNotice 对对象、空对象、数组、null 的处理，用 notifier.SetPendingForTest 构造有更新场景 + captureStdout 劫持输出|json 格式校验 + JSON 编码），被 app list / entity list / relation list / record list / record get 复用
- `skills.go`: skills 命令组，挂载 list / install / update / uninstall / read 子命令；默认 RunE = list（version.go 同款 gh 模式），组级 --output / --all 供裸 `makecli skills` 使用；Args=NoArgs 让未知子命令报错而非静默回落 list（cobra legacyArgs 只查 root）；skillsDoneLine 渲染 install/uninstall 成功句（单复数 skill/skills + 动词），两命令复用
- `skills_list.go`: skills list 子命令，调 skillsync.List 合并本地 lockfile 与 GitHub 远端状态，tablewriter 输出 NAME/STATUS/DESCRIPTION(60 rune 截断)/UPDATED AT(裁到日期)；默认只列已安装（包管理器 list 惯例，uninstall 后条目即消失），--all 才含远端 not installed 条目，表格与 JSON 同一过滤语义；汇总统计永远基于全量清单：默认视图 N installed, M outdated (K more available, --all to show)，--all 视图 N installed, M outdated, K available，均带 update 提示；available 的分母按 [settings] role（resolveRole）的名单过滤——user 只把 makecli 计入可装，未设置全量（summarizeSkills 收 roleNames，nil = 全量）；空态引导经 installFlagForRole 带当前选择（未设置 → `--all --yes` 原有引导，user → `--role user --yes`）；LockWarning/RemoteErr 渲染为 stderr 警告不阻断，退出码恒 0；支持 --output table|json（JSON 保留全文 description 与完整时间戳）；listSkillsFunc 包级可打桩变量
- `skills_list_test.go`: 覆盖 runSkillsList 的单元测试（默认视图隐藏 not-installed+汇总提示 --all/--all 全目录/JSON 默认过滤与 --all 全量/空态引导未设置 role 时仍是 --all/user 角色下名单外的 skills 不计入 available 而名单内的 makecli 计入/空态引导带配置的 role/警告进 stderr 不阻断/非法格式/裸 skills 默认= list/未知子命令报错不回落 list/truncateLine），stubListSkills 打桩隔离 lockfile 与网络并隔离配置目录
- `skills_install.go`: skills install 子命令——三种选择方式互斥（installSelection.resolve 归一为 installPlanArgs{names, all, roleWrite}）：按名选装（不碰 role）/ `--all`（全量，原有行为；roleWrite 清除 [settings] role，回到全量同步）/ `--role user`（名单由 skillsync.RoleSkills 给出：只装 makecli；取值经 settings 键表 role 的 validate 校验，与 settings set role 同一句错；roleWrite 写 [settings] role）；都缺报错指引 skills list；两阶段 skillsync.PlanInstall（npx 门禁 + 远端校验）→ huh confirm 确认（--yes 跳过；非 TTY 拒绝指引 --yes，deploy production 同款护栏）→ **确认后、安装前** 执行 roleWrite（取消不写；安装失败仍留下选择，下次 update 按 role 自愈）→ skillsync.Install；plan.Warning 渲染 stderr；成功输出 skillsDoneLine「name(s) skill(s) installed.」；planInstallFunc / installSkillsFunc / confirmInstallFunc 包级可打桩变量
- `skills_install_test.go`: 覆盖 skills install 的互斥/无参报错/确认流（先 confirm 后 install、拒绝短路、-y 跳过）/警告出 stderr/plan 与 install 错误透传/非 TTY 真门控/子命令注册/plan 原样透传断言 + role：--role user 只 plan makecli 并写 [settings] role、--all 全量并清除已有 role、--all 与 --role 互斥、按名不碰 role、未知 role（含 developer）在 plan 前拒绝且不落盘、--role 带名字互斥、取消确认不写 role、安装失败仍写 role；stubSkillsInstall 三点打桩隔离并隔离配置目录
- `skills_update.go`: skills update 子命令，复用 update.go 的 runSkillSync（skillsync.Sync 幂等：装缺的 + 升级已有的），与 makecli update 后置同步同一代码路径
- `skills_update_test.go`: 覆盖 skills update 走 runSkillSync 且 Skip 恒 false，syncSkillsFunc 打桩避免真实执行 npx
- `skills_uninstall.go`: skills uninstall 子命令（与 install 对仗）——按名移除 / --all 全量互斥（都缺报错指引 skills list），两阶段 skillsync.PlanRemove（npx 门禁 + lockfile 校验/展开，--all 绝不透传上游）→ huh confirm 确认（--yes 跳过；非 TTY 拒绝指引 --yes）→ skillsync.Remove 逐个执行；逐项结果渲染（成功汇总 stdout「name(s) skill(s) uninstalled.」、失败逐行 stderr）；planUninstallFunc / uninstallSkillsFunc / confirmUninstallFunc 包级可打桩变量
- `skills_uninstall_test.go`: 覆盖 skills uninstall 的互斥/无参报错/确认流（先 confirm 后执行、拒绝短路、-y 跳过）/警告出 stderr/逐项失败渲染/plan 与 uninstall 错误透传/非 TTY 真门控，stubSkillsUninstall 三点打桩隔离
- `skills_read.go`: skills read 子命令（SkillContentFS 导出变量：go:embed 不能越出包目录，skills/ submodule 在仓库根，由根包 embed.go 嵌入后 main.go 注入，nil 时报「未嵌入」）——`read <skill>[/<path>] [path]`（args 以 / 拼接交给 skillcontent.Read）：打印二进制内嵌的 SKILL.md（缺省）/ references 文件原文（逐字节写 stdout，agent 可直接消费）/ 目录列举（每行一项）；主文件读毕 stderr 附引用文件读取指引（前置空行分隔）；与 CLI 版本同步、离线可用
- `skills_read_test.go`: 覆盖 skills read 的主文件输出 + stderr 指引、子文件原文无指引、slash/双参/前缀目录+文件三种写法等价、目录列举、未知 skill/未找到错误透传、参数个数校验；stubSkillContent 注入 fstest.MapFS 隔离真实 embed
- `update.go`: update 子命令，支持 [version] 位置参数（v0.2.0 或 0.2.0）、--force、--check、--skip-skills；二进制流程后的 skills 后置同步（runSkillSync）经 resolveRole 读 [settings] role 传给 skillsync.Options.Role，同步命令由 skillsync.SyncCommand(role) 派生并回显——user 只刷 makecli，不把 skills install --role 的选择冲回全量；无 arg 走 CheckLatest 流程（经 resolveChannel 按 [settings] channel 选通道：stable 现状、beta 走列表取 semver 最高，输出经 channelSuffix 回显 [beta channel]），指定版本走 GetRelease（beta tag 天然可装，显式 opt-in 路径）；CompareVersions 决定 upgrade/same/downgrade 分支，降级需 --force；DEV 版本跳过比较；stable 通道下持有高于最新稳定版的预发布版本时 hintBetaAboveStable 补降级去向提示（--force 回稳定或等更高稳定版）；--check 走 runUpdateCheck 仅检查不安装、不同步 skills（有更新打印 current→tag + Release HTMLURL + changelogFileURL 的 CHANGELOG.md 链接 + install 提示，已最新打印 Already up to date），--check 与版本参数互斥；二进制流程结束后默认每次调用 internal/skillsync 同步 Make platform skills，并保留原 binary 输出后追加 skills 输出；applyFunc / syncSkillsFunc 包级变量便于测试打桩
- `update_test.go`: 覆盖 runUpdate / runUpdateCheck 的单元测试（latest 已到位/有更新、specific 升级/同版本/降级拒绝/--force 降级/规范化无 v 前缀/非法 semver/tag 不存在/DEV 跳过比较、skills 输出、--skip-skills 透传；--check 有更新报告/已最新/拒绝带版本参数且不触发 apply/skills；beta 通道走 /releases 列表端点 + [beta channel] 回显、stable 通道持 beta 版本补降级提示；skills 同步按 role 分流：user 角色 Options.Role=user 且回显按名命令），applyFunc / syncSkillsFunc 打桩（stub 按 opts.Role 经 SyncCommand 回填 Command）避免真实替换二进制或执行 npx，cobra SetOut 捕获输出，$MAKE_CLI_CONFIG_DIR 隔离通道与角色配置
- `daemon_service.go`: daemon 的 macOS 托管面——`daemon start / stop / restart / status / uninstall` 五子命令（launchd）；start 先跑真实前置（newEnrolledDaemonFunc：探测本机 coding CLI + 必要时用 setup-key 换回 node key 落盘，失败当场报错而非埋进 KeepAlive 重启循环），再由 buildLaunchdConfig 把「已解析的最终值」固化进 plist——launchd 不继承 shell 环境，故 gateway 地址等写死进 ProgramArguments、PATH/HOME/MAKE_CLI_CONFIG_DIR 进 EnvironmentVariables（缺 PATH 就探测不到 claude/codex），二进制路径经 EvalSymlinks 解到实体；stop 与 uninstall 是两个动词：**stop = bootout + disable 但保留 plist 与 runtime 身份**（disable 不可省，留着 plist 又不 disable 等于下次登录自启，"停了却自己回来"是最坏意外），**uninstall = bootout + enable 清 override + 删 plist + 删除当前 profile 的 node key**（保留日志/工作目录/access token/其他 profile，重新接入必须使用新 setup-key；即使 plist 已不存在也清理残留 node key）；restart 复用磁盘 plist 不重渲染（start 敲定的参数原样保留，未托管则 ErrNotInstalled 转成 `makecli daemon start` 指引）；status 三态 running/stopped/not installed 外加独立的 Autostart enabled/disabled 一行——"现在活没活"与"登录会不会自己回来"是两件正交的事，stopped 的两种成因（被 stop 停用 / 跑挂了）给不同的下一步；key-value 平铺渲染 + `--output table|json`（JSON 的 state 用下划线形态便于脚本判等，另带 disabled 布尔），恒退出码 0；平台守卫 ensureLaunchdSupported 读包级 hostGOOS，非 macOS 明确报错并指回前台形态；launchdInstall/Uninstall/Restart/Query 与 newEnrolledDaemonFunc 均为包级可打桩变量
- `daemon_service_test.go`: 覆盖托管面五命令（start 固化的参数/PATH/配置目录与日志落点、前置失败不装 LaunchAgent、五命令在非 macOS 上一律短路报错、stop 保留托管配置并指出 uninstall 去向、uninstall 交代保留的数据、两者幂等两态、restart 未托管给指引、status 三态 table 渲染 + Autostart 区分 stopped/disabled + JSON 机器可读 + 非法格式拒绝、子命令注册与 flag 继承），stub* 系列打桩 launchd 原语与入册前置，非 macOS 机器也能跑
- `daemon.go`: daemon 顶级子命令（**Hidden**，功能未稳定不对普通用户展示）——以 runtime 模式接入 Agent 平台：探测本机 coding CLI（claude-code / codex）后经 setup-key 自助入册换回 node key（落 ~/.make/credentials 的 Profile.NodeKey，保留同段 access_token），心跳 + claim 轮询驱动执行（编排在 internal/daemon）；flags --gateway-server-url / --setup-key / --name / --work-dir / --max-run-duration；setup-key 与 node key 严格互斥：无本地 key 时用 setup-key 首次入册，有本地 key 时只做正常续连；要换 runtime 身份必须先 daemon uninstall 清 key，再用新的 setup-key；gateway 地址取值链 --gateway-server-url > MAKE_AGENT_SERVER_URL > context preset AgentGatewayURL（随全局 --context，resolveAgentGatewayServerURL 收口，agent 子命令复用；命名与 --meta-server-url/--repo-server-url 一致）；自身 RunE = runDaemon：**缺省即后台**——非 --foreground 且在 macOS 上直接转 runDaemonStart（等价 `daemon start`，写 LaunchAgent 后立刻返回），--foreground 才走 runDaemonForeground 在当前终端阻塞（launchd 拉起的正是这一形态，故 plist 参数必带 --foreground，少了会让托管进程转头再托管自己成无限套娃——daemon_service_test.go 有断言守门）；非 macOS 缺省回落前台并 stderr 明说，不把用户拦在门外。start/stop/restart/status/uninstall 托管子命令在 daemon_service.go，Args=NoArgs 让拼错的子命令报"未知命令"而不是悄悄起一个前台 daemon；flag 用 **PersistentFlags** 定义一次父子共享（start 要用完全同一套旋钮把前台形态固化进 plist），--foreground 例外用本地 Flags（只属于父命令，子命令继承无意义）；daemonRunConfig + resolveDaemonRunConfig（flag/env/preset 一次性解析成最终值）与 newEnrolledDaemon（构造 daemon 并把入册换回的 node key 合并写回 credentials）是前台与托管的共用底座
- `daemon_test.go`: 覆盖 resolveAgentGatewayURL 取值链（三 context preset / env var 覆盖 / flag 最高优先），setContextFlag + MAKE_CLI_CONFIG_DIR 隔离全局态
- `trace.go`: trace 顶级子命令（**Hidden**，内部排障工具）——按 `--id/-i` 与回溯窗口（`--days/-d`、`--hours/-H`（-h 归 help）、`--minutes/-m` 三者相加，全零默认 10 分钟，负值拒绝）拼 OpenObserve `/web/traces/trace-details` 直达 URL（org_identifier/stream 固定 default，from/to 为微秒级 Unix 时间戳，to=now、from=to−窗口），先回显 stdout 再经 openBrowserFunc 打开本地浏览器；基址随全局 --context 取 config.Context.TraceServerURL（dev/test → openobserve.qtech.cn，production → openobserve.qfei.cn）；traceURL/traceWindow 纯函数，nowFunc 包级可打桩
- `trace_test.go`: 覆盖 Hidden 标记、短 flag（-i/-d/-H/-m）、URL 微秒时间戳与 from<to 生成规则、窗口合成（默认/相加/负值）、缺 --id 报错、三 context→OpenObserve 基址映射与 stdout 回显（openBrowserFunc/nowFunc 打桩）
- `agent.go`: agent 顶级子命令（**Hidden**，keyless 通道未公开）——自营脑设备版（agent-design/Design.md §8.2）：LLM 走平台，token 只开模型门，设备端零厂商 key；默认即 code agent（agent.RunCodeOnce/RunCodeREPL：七工具 root=cwd + 目录信任确认 + 两层循环），--chat-only 退回纯聊天 ChatStream（RunOnce/RunREPL）；flags --token（缺省读 MAKE_AGENT_TOKEN，缺失给可操作报错）/ --gateway-url（复用 daemon 的 resolveAgentGatewayURL 取值链）/ --model（平台别名，默认 default）/ -p 一次性模式（无头跑一轮到自然收尾，终结失败转退出码 1）/ --system / --approve（启动即授予 cwd 会话信任，副作用工具免逐次确认）/ --chat-only；无 -p 进交互 REPL（`/exit` `/clear` 外另有 `!<cmd>` 本地命令直通：不发起 LLM 请求直接跑本机 shell，输出流式直写并进历史；-p 无头模式不解析 `!`）；编排在 internal/agent；SilenceUsage 静默 usage 噪音
- `agent_test.go`: 覆盖 agent 命令的单元测试（Hidden 不进 --help / 缺 token 可操作报错 / --approve --chat-only --model flag 注册与缺省值 / 旧 --no-tools 未注册），t.Setenv 隔离环境
- `integration.go`: integration 命令组，挂载 ocr 子命令；预留扩展点供未来其它 integration（translate / asr / embed 等）
- `integration_ocr.go`: integration ocr 子命令，校验文件后缀（.pdf/.ofd/.png/.jpg/.jpeg）后通过 newClientFromProfile 上传，调用 api.OCR；renderOCRTable 风格对齐 entity list <name>：顶部 File/Bills/Took 头部 + 每张票一个 tablewriter LABEL/VALUE 边框表格，按 spec sample 解析 result.pages[].bills[].items[]（过滤空值），断言失败回退 JSON；支持 -f|--file（必选）/ --profile / --output（table|json）/ --business-id / --verify-vat（默认 true，仅 Changed 时显式发送）/ --coord-restore-original / --pages / --crop-complete / --crop-value / --merge-elec / --return-ppi
- `integration_ocr_test.go`: 覆盖 runIntegrationOCR / renderOCRTable 的单元测试（table 渲染 spec sample / json 输出 / 非法扩展名 / 非法格式 / 文件不存在 / 无凭证 / 未知 profile / API 错误 / 异常结构回退），用 httptest 隔离网络

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md

## 命令结构与变更

```text
app.go → clone.go / pull.go → runAppSync → deploy.go / git.go
         clone_test.go（复用 deploy_test.go 的本地仓库夹具）
```

- 2026-09-22：app pull 重命名为 app clone；保留原参数和 ff-only 行为，不注册旧名称别名。
- clone 帮助摘要不展示 fast-forward only 说明，快进保护行为保持不变。
- AGENTS.md 与本文件共享同一模块地图，避免双份文档漂移。
- clone 的 appKey 改为必填，删除工程根目录推断及无参示例。
- 新增独立隐藏 pull 命令：零位置参数，只更新当前工程；clone 必须显式指定 appKey。

- 2026-09-22：configure set/get context 管理 profile 默认后端（空值清除、非法值拒绝）；resolveContext 统一按 flag > env > profile > settings > production 解析。settings/context use 仅修改全局默认。
- context use 的回执明确为全局默认；settings/configure 共享 context 键但保持各自写入范围，相关测试按作用域校验。
