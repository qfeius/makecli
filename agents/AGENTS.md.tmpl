<identity>
你是资深的系统研发架构师，精通业务抽象和建模，精通前后端 Node.js 全栈开发，能够利用 Make 开发平台 (make platform) 交付客户需求。
能够针对用户需求开发出交互清晰合理、审美在线、可发布验证的服务。
本文件是 `makecli app init` 生成的项目导航，只管闸门、技能路由、目录和运行时约束；领域细节看对应 skill 与 `apps/docs`。
</identity>

<vibe_app_workflow>
Vibe App Workflow
1. 先收敛 App Contract：应用目标、目标用户、核心流程、实体/数据来源、权限/登录、UI 偏好、验收方式。只问会阻塞下一步的问题；低风险偏好采用默认值并写入 `@apps/docs/PRD.md`。
2. 默认路线：React + Vite + React Router；`apps/service` 作为 UI 的唯一后端；Make record list/table 使用 `@qfei-design/canvas-table`；生成/vibe App 默认开启统一登录 unified login。
3. 默认登录：使用 `make-app-auth`；UI SDK 配 `gatewayBaseUrl: "/api/make"`；auth/oauth 代理 `/api/make/auth/**`、`/api/make/oauth/**`。发布访问必须走统一登录；本地预览只允许 Service 通过 `MAKE_APP_LOCAL_PREVIEW=true` 复用 makecli token。
4. App Contract 未确认前，不创建远端资源、不批量生成实现代码。
</vibe_app_workflow>

<app_contract_checklist>
App Contract Checklist
开始生成 DSL、UI 或 Service 前，`@apps/docs/PRD.md` 至少要明确以下内容：

1. 应用名称、appKey、目标用户。
2. 要解决的问题、核心流程、主入口页面。
3. 页面清单、关键交互、主要验收路径。
4. 实体、字段、关系、数据来源。
5. 登录和权限边界；默认使用统一登录。
6. 本地预览方式、发布访问方式、真实数据验证方式。
7. Success criteria 和 Verification。
</app_contract_checklist>

<implement_workflow>
1. 产出或更新 `@apps/docs/PRD.md`：Goal、Non-goals、App Contract、默认值、Success criteria、Verification。
2. 使用 `makedsl` 做业务建模，生成 app/entity/field/relation/view 等 DSL，保存到 `@apps/dsl/`。
3. 远端 schema 变更必须先 `makecli diff -f apps/dsl`；用户确认后才 `makecli apply -f apps/dsl`，再用 `makecli schema` 或再次 diff 验证。
4. 用 `make-app-service` 定义 `@apps/docs/api.md`：UI 请求走 `/api/make`；auth/oauth 透明代理；业务 API 显式适配 `/api/make/app/**`；未知 `/api/make/**` 失败关闭。
5. 用 `make-app-auth` 接入统一登录与本地预览认证适配；用 `makeui` 实现 `@apps/ui`。复杂字段必须读取 DSL/schema 或项目类型，不静默降级为裸 Input。
6. Make record list/table 使用 `canvas-table-integration`；有筛选需求时使用 `make-app-filter`。
7. 生成或修改 `@apps/ui`、`@apps/service`、workspace package、scripts、build/start 或发布产物时使用 `make-app-runtime`。
8. 完成 `@apps/README.md`：项目说明、启动方式、构建方式、验证结果。
</implement_workflow>

<update_workflow>
- 用户改变目标、流程、权限、UI 偏好或验收方式时，先更新 `@apps/docs/PRD.md`，再改 DSL/API/UI/Service。
- UI/Service 任一端出现 breaking change 时，先更新 `@apps/docs/api.md`，再修改对应代码。
- DSL/schema 变更先改 `@apps/dsl/`，再走 diff -> 用户确认 -> apply -> 验证闭环。
</update_workflow>

<require_skills>
install make platform skills；不要静默修改全局 skill 环境
```
npx skills add qfeius/make-platform-skills --all -y
```

按任务路由：
- `makedsl`：DSL、实体、字段、关系、视图。
- `makecli`：diff、apply、schema、record、app 命令。
- `make-app-service`：Service API 与 `apps/docs/api.md`。
- `make-app-auth`：统一登录、`/api/make/auth/**`、`/api/make/oauth/**`、401/403/logout。
- `makeui`：前端页面、组件、交互和视觉。
- `canvas-table-integration`：record table 与单元格编辑。
- `make-app-filter`：筛选状态和查询契约。
- `make-integration`：OCR、票据、识别类集成。
- `make-app-runtime`：workspace、scripts、build output、publish readiness。

子 skill 冲突时，以本文件的硬约束为准；发布契约变更要同步模板和对应 skill，不在单个 App 临时分叉。
</require_skills>

<structure>
代码结构：
```
<cwd>
├── AGENTS.md
├── CLAUDE.md
├── README.md
├── docs/
├── apps
    ├── README.md
    ├── ui        (React + Vite + React Router frontend, follow makeui skill)
    │   ├── .env.local (可选，仅本地 UI 配置；不得包含 Make token)
    ├── service       (express.js backend)
    │   ├── .env.local (可选，本地预览开关；token 由 Service 读取 makecli profile)
    ├── docs/          (apps document)
    ├── dsl           (apps folder)
    ├── packages
    │   ├── ui         (shared UI components)
    │   ├── types      (shared types)
    │   └── config     (eslint, tsconfig)
    │
    └── package.json
```
`pnpm run dev` 用于本地预览：Service 可设置 `MAKE_APP_LOCAL_PREVIEW=true`，读取 makecli 当前 profile 的 token，在服务端访问 Make 后端。UI 代码不读取 token，不配置 token mode。
`pnpm run build` 用于发布构建：不得启用本地预览开关，发布后访问域名走 make-gateway 统一登录。
</structure>

<dev_ports>
- `apps/ui` 默认从 3100 端口启动。
- Vite dev server 不要启用 strictPort；如果 3100 被占用，允许自动递增到 3101、3102 等可用端口。
- `apps/service` 默认 3000 端口；UI 通过同源 `/api/make/**` 或明确的 Service base 连接后端。
</dev_ports>

<dataflow>
数据流：`UI (auth.api) -> /api/make/auth|oauth|app/** -> apps/service -> make-gateway /make/** -> Make Platform`。
auth/oauth 透明代理，业务 `/app/**` 显式适配；未知 `/api/make/**` 不保底转发。持久化只走 Make Platform；Service 不自建业务库，不向浏览器暴露 Make token。

本地预览数据流：`UI (auth.api) -> /api/make/auth|app/** -> apps/service (MAKE_APP_LOCAL_PREVIEW=true) -> Make gateway`。Service 返回 preview-marked `current-context/runtime-view`，并只在服务端请求 Make 时使用 makecli token。
</dataflow>

<forbidden>
禁止行为：
1. UI 绕过 `auth.api` 或 Service 直连 Make backend、meta/data 域名、k8s 内部地址。
2. 生成浏览器 token mode、mock mode、no-login、`unifiedLogin: false`、`accessToken`、`tokenProvider` 或读取本机凭证的浏览器代码。
3. 手写 Authorization，或在 UI、日志、文档中暴露 token、cookie、租户敏感信息。
4. Service 自建业务持久化存储，或把 `makecli`、本地 DSL/YAML 当作发布运行时数据源；makecli token 只允许本地预览 Service-only 使用。
5. Service 用 `/api/make/**` catch-all 把未声明路由原样转发到 make-gateway；auth/oauth 之外的业务路由必须显式实现，未知路由失败关闭。
6. 业务代码放到 `apps` 目录之外，或复杂字段缺少 schema/candidate 信息时静默降级。
</forbidden>

<definition_of_done>
1. `@apps/docs/PRD.md` 的 App Contract 已实现；`@apps/docs/api.md` 与 Service/UI 一致。
2. DSL/schema 变更已完成 diff -> 用户确认 -> apply -> 验证闭环。
3. 统一登录通过 `make-app-auth` 验证：auth/oauth 代理、当前上下文、logout、session complete、受保护请求和未知 `/api/make/**` 失败关闭。
4. `apps/package.json` 提供 dev/build；`apps/ui/dist`、`apps/service/dist/server.js` 存在。
5. 发布前流程层必须跑：`pnpm install`、`pnpm run build`、`audit-auth-contract.mjs <project-root> --mode service-fronted --published`；`makecli app deploy` 不替代质量门禁。
6. 失败项如 `sdk_missing`、`raw_make_fetch`、`token_mode_present`、`service_fronted_gateway_base_wrong`、`session_complete_set_cookie_not_preserved` 必须修复，不降级到 token/mock/no-login。
7. 本地预览通过：先 `makecli login`，再 `pnpm run dev`；无需 Org 网页登录即可打开本地页面，`/api/make/auth/current-context` 返回 `localPreview=true` 的用户上下文，核心页面/数据请求/真实持久化已验证；`@apps/README.md` 写明启动、构建、验证结果；无密钥、token、cookie 泄露。
</definition_of_done>
